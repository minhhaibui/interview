/*
 * Bộ bài luyện TƯ DUY LẬP TRÌNH — các bài phỏng vấn kinh điển (Việt hóa).
 * Mỗi bài: viết hàm JS, app chạy test thật trong trình duyệt (Web Worker sandbox).
 * fnName phải khớp tên hàm trong starter/solution. tests: { args:[...], expected }.
 * So sánh kết quả bằng JSON.stringify (chú ý: với bài không quan tâm thứ tự thì expected đã chuẩn hóa).
 */
window.CODING_PROBLEMS = [
  {
    id: 'two-sum', title: 'Two Sum — tìm 2 số cộng lại bằng target',
    topic: 'Hash Map', difficulty: 'Dễ', fnName: 'twoSum',
    prompt: 'Cho mảng số nguyên `nums` và số `target`. Trả về mảng `[i, j]` là **chỉ số** của hai phần tử cộng lại bằng `target` (i < j). Đề bảo đảm có đúng một đáp án, không dùng lại cùng một phần tử.',
    starter: 'function twoSum(nums, target) {\n  // code của bạn ở đây\n\n}',
    tests: [
      { args: [[2, 7, 11, 15], 9], expected: [0, 1] },
      { args: [[3, 2, 4], 6], expected: [1, 2] },
      { args: [[3, 3], 6], expected: [0, 1] },
      { args: [[-1, -2, -3, -4, -5], -8], expected: [2, 4] },
    ],
    hints: [
      'Cách ngây thơ là 2 vòng lặp lồng nhau O(n²). Có cách O(n) không?',
      'Dùng một object/Map lưu {giá trị đã thấy : chỉ số của nó}.',
      'Với mỗi số x ở vị trí i, kiểm tra xem (target - x) đã từng xuất hiện chưa.',
    ],
    solution: 'function twoSum(nums, target) {\n  const seen = {};\n  for (let i = 0; i < nums.length; i++) {\n    const need = target - nums[i];\n    if (need in seen) return [seen[need], i];\n    seen[nums[i]] = i;\n  }\n}',
    explain: 'Duyệt một lần. Với mỗi số, ta cần tìm "số bù" target - x. Lưu các số đã gặp vào hash map để tra cứu O(1) → tổng O(n) thời gian, O(n) bộ nhớ.',
  },
  {
    id: 'reverse-string', title: 'Đảo ngược chuỗi',
    topic: 'Chuỗi', difficulty: 'Dễ', fnName: 'reverseStr',
    prompt: 'Viết hàm `reverseStr(s)` trả về chuỗi `s` đảo ngược. Ví dụ `"hello"` → `"olleh"`.',
    starter: 'function reverseStr(s) {\n\n}',
    tests: [
      { args: ['hello'], expected: 'olleh' },
      { args: ['abc'], expected: 'cba' },
      { args: [''], expected: '' },
      { args: ['a'], expected: 'a' },
    ],
    hints: [
      'Có thể tách thành mảng ký tự, đảo, rồi nối lại.',
      'Hoặc dùng vòng lặp duyệt từ cuối về đầu, nối dần.',
    ],
    solution: "function reverseStr(s) {\n  return s.split('').reverse().join('');\n}",
    explain: 'split("") → mảng ký tự, reverse() đảo tại chỗ, join("") nối lại. Cách tự cài: vòng for từ s.length-1 về 0 nối từng ký tự.',
  },
  {
    id: 'fizzbuzz', title: 'FizzBuzz',
    topic: 'Logic', difficulty: 'Dễ', fnName: 'fizzbuzz',
    prompt: 'Trả về **mảng** gồm n phần tử (1..n). Với mỗi số: chia hết cho 3 → `"Fizz"`, chia hết cho 5 → `"Buzz"`, chia hết cả 3 và 5 → `"FizzBuzz"`, còn lại → chuỗi của chính số đó.',
    starter: 'function fizzbuzz(n) {\n\n}',
    tests: [
      { args: [5], expected: ['1', '2', 'Fizz', '4', 'Buzz'] },
      { args: [3], expected: ['1', '2', 'Fizz'] },
      { args: [15], expected: ['1', '2', 'Fizz', '4', 'Buzz', 'Fizz', '7', '8', 'Fizz', 'Buzz', '11', 'Fizz', '13', '14', 'FizzBuzz'] },
    ],
    hints: [
      'Kiểm tra điều kiện chia hết cả 3 VÀ 5 TRƯỚC, nếu không sẽ bị "Fizz" che mất.',
      'Dùng toán tử % (chia lấy dư). i % 3 === 0 nghĩa là chia hết cho 3.',
    ],
    solution: "function fizzbuzz(n) {\n  const out = [];\n  for (let i = 1; i <= n; i++) {\n    if (i % 15 === 0) out.push('FizzBuzz');\n    else if (i % 3 === 0) out.push('Fizz');\n    else if (i % 5 === 0) out.push('Buzz');\n    else out.push(String(i));\n  }\n  return out;\n}",
    explain: 'Mẹo kinh điển: xét i % 15 (bội chung của 3 và 5) trước. Bài kiểm tra khả năng sắp xếp thứ tự điều kiện.',
  },
  {
    id: 'palindrome', title: 'Kiểm tra chuỗi đối xứng (palindrome)',
    topic: 'Chuỗi', difficulty: 'Dễ', fnName: 'isPalindrome',
    prompt: 'Trả về `true` nếu chuỗi đọc xuôi và ngược giống nhau, **bỏ qua hoa/thường và mọi ký tự không phải chữ-số**. Ví dụ `"A man, a plan, a canal: Panama"` → `true`.',
    starter: 'function isPalindrome(s) {\n\n}',
    tests: [
      { args: ['A man, a plan, a canal: Panama'], expected: true },
      { args: ['race a car'], expected: false },
      { args: [''], expected: true },
      { args: ['Was it a car or a cat I saw?'], expected: true },
    ],
    hints: [
      'Bước 1: chuẩn hóa — bỏ ký tự không phải chữ/số, đưa về chữ thường.',
      'Dùng regex /[^a-z0-9]/g để loại ký tự thừa sau khi toLowerCase().',
      'Sau khi chuẩn hóa, so sánh chuỗi với bản đảo ngược của nó.',
    ],
    solution: "function isPalindrome(s) {\n  const c = s.toLowerCase().replace(/[^a-z0-9]/g, '');\n  return c === c.split('').reverse().join('');\n}",
    explain: 'Làm sạch dữ liệu trước rồi so sánh với bản đảo. Cách hai con trỏ (đầu/cuối tiến vào giữa) tiết kiệm bộ nhớ hơn.',
  },
  {
    id: 'max-subarray', title: 'Tổng mảng con lớn nhất (Kadane)',
    topic: 'Mảng / DP', difficulty: 'Trung bình', fnName: 'maxSubArray',
    prompt: 'Cho mảng số nguyên (có thể âm). Tìm **tổng lớn nhất** của một mảng con liên tiếp (ít nhất 1 phần tử). Ví dụ `[-2,1,-3,4,-1,2,1,-5,4]` → `6` (đoạn `[4,-1,2,1]`).',
    starter: 'function maxSubArray(nums) {\n\n}',
    tests: [
      { args: [[-2, 1, -3, 4, -1, 2, 1, -5, 4]], expected: 6 },
      { args: [[1]], expected: 1 },
      { args: [[-1, -2, -3]], expected: -1 },
      { args: [[5, 4, -1, 7, 8]], expected: 23 },
    ],
    hints: [
      'Brute force O(n²) xét mọi đoạn. Có cách O(n) duyệt một lần (thuật toán Kadane).',
      'Tại mỗi phần tử, hỏi: "nối vào đoạn trước có lợi không, hay bắt đầu đoạn mới từ đây?"',
      'cur = max(nums[i], cur + nums[i]); best = max(best, cur).',
    ],
    solution: 'function maxSubArray(nums) {\n  let cur = nums[0], best = nums[0];\n  for (let i = 1; i < nums.length; i++) {\n    cur = Math.max(nums[i], cur + nums[i]);\n    best = Math.max(best, cur);\n  }\n  return best;\n}',
    explain: 'Kadane: cur là tổng tốt nhất của đoạn KẾT THÚC tại i. Nếu cur đang âm thì vứt đi, bắt đầu lại. best lưu kỷ lục toàn cục.',
  },
  {
    id: 'fibonacci', title: 'Số Fibonacci thứ n',
    topic: 'Đệ quy / DP', difficulty: 'Dễ', fnName: 'fib',
    prompt: 'Trả về số Fibonacci thứ n, với `fib(0) = 0`, `fib(1) = 1`, `fib(n) = fib(n-1) + fib(n-2)`. **Chú ý:** đệ quy thuần sẽ rất chậm với n lớn — hãy nghĩ cách lặp.',
    starter: 'function fib(n) {\n\n}',
    tests: [
      { args: [0], expected: 0 },
      { args: [1], expected: 1 },
      { args: [10], expected: 55 },
      { args: [30], expected: 832040 },
    ],
    hints: [
      'Đệ quy fib(n-1)+fib(n-2) đúng nhưng O(2ⁿ) — n=40 đã treo máy.',
      'Chỉ cần nhớ HAI số trước đó, lặp từ dưới lên: O(n).',
    ],
    solution: 'function fib(n) {\n  if (n < 2) return n;\n  let a = 0, b = 1;\n  for (let i = 2; i <= n; i++) { [a, b] = [b, a + b]; }\n  return b;\n}',
    explain: 'Bottom-up: chỉ giữ hai biến a, b cuộn dần. Đây là ví dụ kinh điển cho thấy "đúng" chưa đủ — phải "đủ nhanh".',
  },
  {
    id: 'anagram', title: 'Hai chuỗi có phải đảo chữ của nhau (anagram)?',
    topic: 'Hash Map', difficulty: 'Trung bình', fnName: 'isAnagram',
    prompt: 'Trả về `true` nếu `t` là hoán vị các ký tự của `s` (cùng tập ký tự, cùng số lượng). Ví dụ `("anagram","nagaram")` → `true`, `("rat","car")` → `false`.',
    starter: 'function isAnagram(s, t) {\n\n}',
    tests: [
      { args: ['anagram', 'nagaram'], expected: true },
      { args: ['rat', 'car'], expected: false },
      { args: ['', ''], expected: true },
      { args: ['ab', 'a'], expected: false },
    ],
    hints: [
      'Nếu độ dài khác nhau → chắc chắn false (kiểm tra sớm).',
      'Đếm tần suất ký tự của s, rồi trừ dần theo t; hoặc sắp xếp cả hai rồi so sánh.',
    ],
    solution: "function isAnagram(s, t) {\n  if (s.length !== t.length) return false;\n  const cnt = {};\n  for (const ch of s) cnt[ch] = (cnt[ch] || 0) + 1;\n  for (const ch of t) {\n    if (!cnt[ch]) return false;\n    cnt[ch]--;\n  }\n  return true;\n}",
    explain: 'Đếm tần suất bằng hash map: O(n). Cách sắp xếp O(n log n) ngắn hơn nhưng chậm hơn. Luôn kiểm tra độ dài trước.',
  },
  {
    id: 'unique-array', title: 'Loại bỏ phần tử trùng (giữ thứ tự)',
    topic: 'Mảng', difficulty: 'Dễ', fnName: 'unique',
    prompt: 'Trả về mảng mới đã bỏ các phần tử trùng, **giữ nguyên thứ tự xuất hiện đầu tiên**. Ví dụ `[1,1,2,3,3,2]` → `[1,2,3]`.',
    starter: 'function unique(arr) {\n\n}',
    tests: [
      { args: [[1, 1, 2, 3, 3, 2]], expected: [1, 2, 3] },
      { args: [[]], expected: [] },
      { args: [[5, 5, 5]], expected: [5] },
      { args: [[1, 2, 3]], expected: [1, 2, 3] },
    ],
    hints: [
      'Dùng Set để biết phần tử đã gặp chưa.',
      '[...new Set(arr)] tự khử trùng và giữ thứ tự chèn.',
    ],
    solution: 'function unique(arr) {\n  return [...new Set(arr)];\n}',
    explain: 'Set chỉ giữ giá trị duy nhất và bảo toàn thứ tự chèn trong JS. Cách thủ công: duyệt + một Set "đã thấy" + push nếu chưa có.',
  },
  {
    id: 'missing-number', title: 'Tìm số còn thiếu trong 0..n',
    topic: 'Mảng / Toán', difficulty: 'Trung bình', fnName: 'missingNumber',
    prompt: 'Mảng chứa n số **khác nhau** lấy từ tập `0,1,...,n` (thiếu đúng một số). Tìm số bị thiếu. Ví dụ `[3,0,1]` (n=3) → `2`.',
    starter: 'function missingNumber(nums) {\n\n}',
    tests: [
      { args: [[3, 0, 1]], expected: 2 },
      { args: [[0, 1]], expected: 2 },
      { args: [[9, 6, 4, 2, 3, 5, 7, 0, 1]], expected: 8 },
      { args: [[0]], expected: 1 },
    ],
    hints: [
      'Tổng của 0..n là n*(n+1)/2. So với tổng thực tế của mảng.',
      'Hiệu giữa tổng kỳ vọng và tổng thực tế chính là số thiếu — O(n), không cần bộ nhớ phụ.',
    ],
    solution: 'function missingNumber(nums) {\n  const n = nums.length;\n  const expected = n * (n + 1) / 2;\n  const actual = nums.reduce((s, x) => s + x, 0);\n  return expected - actual;\n}',
    explain: 'Mẹo toán: tổng Gauss 0..n trừ tổng mảng = số thiếu. Cách XOR cũng hay (a^a=0) và tránh tràn số.',
  },
  {
    id: 'binary-search', title: 'Tìm kiếm nhị phân',
    topic: 'Tìm kiếm', difficulty: 'Trung bình', fnName: 'search',
    prompt: 'Cho mảng **đã sắp tăng** và `target`. Trả về chỉ số của target, hoặc `-1` nếu không có. Yêu cầu O(log n).',
    starter: 'function search(nums, target) {\n\n}',
    tests: [
      { args: [[-1, 0, 3, 5, 9, 12], 9], expected: 4 },
      { args: [[-1, 0, 3, 5, 9, 12], 2], expected: -1 },
      { args: [[5], 5], expected: 0 },
      { args: [[], 1], expected: -1 },
    ],
    hints: [
      'Giữ hai biên lo, hi. Lặp khi lo <= hi.',
      'mid = (lo+hi) >> 1 (chia đôi). So nums[mid] với target để bỏ một nửa.',
      'Cẩn thận điều kiện dừng và cập nhật lo = mid+1 / hi = mid-1 để không lặp vô tận.',
    ],
    solution: 'function search(nums, target) {\n  let lo = 0, hi = nums.length - 1;\n  while (lo <= hi) {\n    const mid = (lo + hi) >> 1;\n    if (nums[mid] === target) return mid;\n    if (nums[mid] < target) lo = mid + 1;\n    else hi = mid - 1;\n  }\n  return -1;\n}',
    explain: 'Mỗi bước loại nửa không gian tìm kiếm → O(log n). Lỗi hay gặp: dùng < thay vì <=, hoặc quên +1/-1 gây vòng lặp vô tận.',
  },
  {
    id: 'valid-parentheses', title: 'Dấu ngoặc hợp lệ (Stack)',
    topic: 'Stack', difficulty: 'Trung bình', fnName: 'isValid',
    prompt: 'Chuỗi chỉ gồm `()[]{}`. Trả về `true` nếu mọi ngoặc đóng/mở đúng loại và đúng thứ tự. Ví dụ `"()[]{}"` → `true`, `"([)]"` → `false`.',
    starter: 'function isValid(s) {\n\n}',
    tests: [
      { args: ['()'], expected: true },
      { args: ['()[]{}'], expected: true },
      { args: ['(]'], expected: false },
      { args: ['([)]'], expected: false },
      { args: ['{[]}'], expected: true },
      { args: ['('], expected: false },
      { args: [''], expected: true },
    ],
    hints: [
      'Đây là bài toán kinh điển dùng NGĂN XẾP (stack).',
      'Gặp ngoặc mở → push. Gặp ngoặc đóng → phải khớp với phần tử trên đỉnh stack, nếu không thì false.',
      'Cuối cùng stack phải rỗng (không còn ngoặc mở chưa đóng).',
    ],
    solution: "function isValid(s) {\n  const pair = { ')': '(', ']': '[', '}': '{' };\n  const st = [];\n  for (const ch of s) {\n    if (ch === '(' || ch === '[' || ch === '{') st.push(ch);\n    else if (st.pop() !== pair[ch]) return false;\n  }\n  return st.length === 0;\n}",
    explain: 'Stack mô hình hóa "đóng cái mở gần nhất trước". Map ngoặc-đóng→ngoặc-mở giúp so khớp gọn. Đừng quên kiểm tra stack rỗng ở cuối.',
  },
  {
    id: 'move-zeroes', title: 'Dồn số 0 về cuối mảng',
    topic: 'Hai con trỏ', difficulty: 'Trung bình', fnName: 'moveZeroes',
    prompt: 'Trả về mảng với mọi số `0` được dồn về **cuối**, các số khác **giữ nguyên thứ tự**. Ví dụ `[0,1,0,3,12]` → `[1,3,12,0,0]`.',
    starter: 'function moveZeroes(nums) {\n\n}',
    tests: [
      { args: [[0, 1, 0, 3, 12]], expected: [1, 3, 12, 0, 0] },
      { args: [[0]], expected: [0] },
      { args: [[1, 2, 3]], expected: [1, 2, 3] },
      { args: [[0, 0, 1]], expected: [1, 0, 0] },
    ],
    hints: [
      'Cách dễ: lọc các số khác 0 ra trước, rồi thêm đủ số 0 vào cuối.',
      'Cách "tại chỗ": dùng một con trỏ ghi (write index), đẩy các số khác 0 lên đầu rồi điền 0 phần còn lại.',
    ],
    solution: 'function moveZeroes(nums) {\n  const nonZero = nums.filter(x => x !== 0);\n  const zeros = nums.length - nonZero.length;\n  return nonZero.concat(Array(zeros).fill(0));\n}',
    explain: 'Tách phần khác 0 (giữ thứ tự) rồi nối thêm các số 0. Bản phỏng vấn thường yêu cầu làm TẠI CHỖ O(1) bộ nhớ bằng hai con trỏ.',
  },
  {
    id: 'count-primes', title: 'Đếm số nguyên tố nhỏ hơn n',
    topic: 'Toán', difficulty: 'Trung bình', fnName: 'countPrimes',
    prompt: 'Đếm số lượng số nguyên tố **nhỏ hơn** n (không tính n). Ví dụ `countPrimes(10)` → `4` (2,3,5,7).',
    starter: 'function countPrimes(n) {\n\n}',
    tests: [
      { args: [10], expected: 4 },
      { args: [0], expected: 0 },
      { args: [2], expected: 0 },
      { args: [20], expected: 8 },
      { args: [100], expected: 25 },
    ],
    hints: [
      'Kiểm tra từng số bằng vòng lặp tới √x cũng được, nhưng có cách nhanh hơn.',
      'Sàng Eratosthenes: đánh dấu mọi bội của số nguyên tố là "không nguyên tố".',
      'Chỉ cần quét i tới √n khi gạch bội số.',
    ],
    solution: 'function countPrimes(n) {\n  if (n < 3) return 0;\n  const sieve = new Array(n).fill(true);\n  sieve[0] = sieve[1] = false;\n  for (let i = 2; i * i < n; i++) {\n    if (sieve[i]) {\n      for (let j = i * i; j < n; j += i) sieve[j] = false;\n    }\n  }\n  return sieve.filter(Boolean).length;\n}',
    explain: 'Sàng Eratosthenes: O(n log log n). Bắt đầu gạch từ i*i (các bội nhỏ hơn đã bị gạch bởi số nguyên tố nhỏ hơn).',
  },
  {
    id: 'merge-sorted', title: 'Trộn hai mảng đã sắp xếp',
    topic: 'Hai con trỏ', difficulty: 'Trung bình', fnName: 'merge',
    prompt: 'Cho hai mảng `a`, `b` đã sắp tăng. Trả về một mảng mới gộp cả hai, **vẫn sắp tăng**. Ví dụ `([1,2,4],[1,3,4])` → `[1,1,2,3,4,4]`.',
    starter: 'function merge(a, b) {\n\n}',
    tests: [
      { args: [[1, 2, 4], [1, 3, 4]], expected: [1, 1, 2, 3, 4, 4] },
      { args: [[], [0]], expected: [0] },
      { args: [[1, 2, 3], []], expected: [1, 2, 3] },
      { args: [[2, 5], [1, 3, 6]], expected: [1, 2, 3, 5, 6] },
    ],
    hints: [
      'Có thể nối rồi sort O((m+n)log(m+n)) — nhưng phí vì hai mảng đã sắp sẵn.',
      'Hai con trỏ i, j chạy song song, mỗi bước lấy phần tử nhỏ hơn — O(m+n).',
      'Nhớ xử lý phần còn dư khi một mảng hết trước.',
    ],
    solution: 'function merge(a, b) {\n  const out = [];\n  let i = 0, j = 0;\n  while (i < a.length && j < b.length) {\n    if (a[i] <= b[j]) out.push(a[i++]);\n    else out.push(b[j++]);\n  }\n  while (i < a.length) out.push(a[i++]);\n  while (j < b.length) out.push(b[j++]);\n  return out;\n}',
    explain: 'Kỹ thuật "merge" trong merge sort: hai con trỏ tiến song song, luôn lấy đầu nhỏ hơn. Đây là nền tảng của sắp xếp trộn.',
  },
  {
    id: 'group-anagrams', title: 'Nhóm các từ đảo chữ (anagram)',
    topic: 'Hash Map', difficulty: 'Trung bình', fnName: 'groupAnagrams',
    prompt: 'Cho mảng chuỗi `words`. Gom các từ là **đảo chữ của nhau** (cùng tập chữ cái) vào chung nhóm. Trả về mảng các nhóm; **mỗi nhóm sắp xếp tăng dần**, và **các nhóm cũng sắp tăng theo phần tử đầu**. Ví dụ `["eat","tea","tan","ate","nat","bat"]` → `[["ate","eat","tea"],["bat"],["nat","tan"]]`.',
    starter: 'function groupAnagrams(words) {\n\n}',
    tests: [
      { args: [['eat', 'tea', 'tan', 'ate', 'nat', 'bat']], expected: [['ate', 'eat', 'tea'], ['bat'], ['nat', 'tan']] },
      { args: [['']], expected: [['']] },
      { args: [['a']], expected: [['a']] },
      { args: [['abc', 'cba', 'xyz']], expected: [['abc', 'cba'], ['xyz']] },
    ],
    hints: [
      'Hai từ là anagram khi sắp xếp các ký tự lại thì giống nhau — dùng chuỗi đã sort làm khoá.',
      'Dùng một Map { khoá-đã-sort : [danh sách từ] }.',
      'Cuối cùng sort từng nhóm, rồi sort danh sách nhóm theo phần tử đầu để khớp đáp án.',
    ],
    solution: 'function groupAnagrams(words) {\n  const map = {};\n  for (const w of words) {\n    const key = w.split("").sort().join("");\n    (map[key] ||= []).push(w);\n  }\n  return Object.values(map)\n    .map(g => g.sort())\n    .sort((a, b) => a[0] < b[0] ? -1 : a[0] > b[0] ? 1 : 0);\n}',
    explain: 'Chuẩn hoá mỗi từ bằng cách sort ký tự → các anagram có cùng khoá. Gom theo khoá trong hash map: O(n·k log k) với k là độ dài từ.',
  },
  {
    id: 'contains-duplicate', title: 'Mảng có phần tử trùng không?',
    topic: 'Hash Set', difficulty: 'Dễ', fnName: 'containsDuplicate',
    prompt: 'Trả về `true` nếu mảng `nums` có **bất kỳ giá trị nào xuất hiện ≥ 2 lần**, ngược lại `false`. Ví dụ `[1,2,3,1]` → `true`, `[1,2,3]` → `false`.',
    starter: 'function containsDuplicate(nums) {\n\n}',
    tests: [
      { args: [[1, 2, 3, 1]], expected: true },
      { args: [[1, 2, 3, 4]], expected: false },
      { args: [[]], expected: false },
      { args: [[2, 2]], expected: true },
    ],
    hints: [
      'Có thể sort rồi so sánh hai phần tử kề nhau — O(n log n).',
      'Nhanh hơn: dùng một Set, vừa duyệt vừa kiểm tra đã thấy chưa — O(n).',
    ],
    solution: 'function containsDuplicate(nums) {\n  const seen = new Set();\n  for (const x of nums) {\n    if (seen.has(x)) return true;\n    seen.add(x);\n  }\n  return false;\n}',
    explain: 'Set cho phép kiểm tra "đã gặp chưa" trong O(1). Gặp phần tử đã có trong Set → có trùng. O(n) thời gian, O(n) bộ nhớ.',
  },
  {
    id: 'single-number', title: 'Số chỉ xuất hiện một lần (XOR)',
    topic: 'Bit', difficulty: 'Trung bình', fnName: 'singleNumber',
    prompt: 'Mọi phần tử xuất hiện **đúng 2 lần**, trừ **một** phần tử xuất hiện 1 lần. Tìm phần tử đó. Yêu cầu O(n) thời gian, O(1) bộ nhớ. Ví dụ `[4,1,2,1,2]` → `4`.',
    starter: 'function singleNumber(nums) {\n\n}',
    tests: [
      { args: [[2, 2, 1]], expected: 1 },
      { args: [[4, 1, 2, 1, 2]], expected: 4 },
      { args: [[7]], expected: 7 },
      { args: [[0, 1, 0]], expected: 1 },
    ],
    hints: [
      'Phép XOR: a ^ a = 0 và a ^ 0 = a.',
      'XOR tất cả phần tử lại — các cặp triệt tiêu nhau, còn lại số lẻ.',
    ],
    solution: 'function singleNumber(nums) {\n  let acc = 0;\n  for (const x of nums) acc ^= x;\n  return acc;\n}',
    explain: 'XOR có tính giao hoán & a^a=0, nên mọi số xuất hiện 2 lần triệt tiêu, chỉ còn số duy nhất. O(n) thời gian, O(1) bộ nhớ — không cần Set.',
  },
  {
    id: 'climbing-stairs', title: 'Đếm cách leo cầu thang',
    topic: 'Quy hoạch động', difficulty: 'Trung bình', fnName: 'climbStairs',
    prompt: 'Có `n` bậc thang, mỗi bước leo **1 hoặc 2** bậc. Hỏi có bao nhiêu cách leo lên đỉnh? Ví dụ `n=3` → `3` (1+1+1, 1+2, 2+1).',
    starter: 'function climbStairs(n) {\n\n}',
    tests: [
      { args: [2], expected: 2 },
      { args: [3], expected: 3 },
      { args: [5], expected: 8 },
      { args: [1], expected: 1 },
    ],
    hints: [
      'Để tới bậc n, bạn đến từ bậc n-1 (bước 1) hoặc n-2 (bước 2).',
      'ways(n) = ways(n-1) + ways(n-2) — chính là Fibonacci.',
    ],
    solution: 'function climbStairs(n) {\n  let a = 1, b = 1;\n  for (let i = 0; i < n; i++) {\n    const t = a + b;\n    a = b; b = t;\n  }\n  return a;\n}',
    explain: 'Số cách leo tới bậc n bằng tổng cách tới n-1 và n-2 → dãy Fibonacci. Lưu 2 biến thay vì mảng → O(1) bộ nhớ.',
  },
  {
    id: 'max-profit', title: 'Lợi nhuận tối đa mua bán cổ phiếu',
    topic: 'Tham lam', difficulty: 'Trung bình', fnName: 'maxProfit',
    prompt: 'Mảng `prices[i]` là giá ngày i. Chỉ được **mua một lần rồi bán một lần sau đó**. Trả về lợi nhuận lớn nhất (0 nếu không có lãi). Ví dụ `[7,1,5,3,6,4]` → `5` (mua 1, bán 6).',
    starter: 'function maxProfit(prices) {\n\n}',
    tests: [
      { args: [[7, 1, 5, 3, 6, 4]], expected: 5 },
      { args: [[7, 6, 4, 3, 1]], expected: 0 },
      { args: [[1, 2]], expected: 1 },
      { args: [[]], expected: 0 },
    ],
    hints: [
      'Duyệt một lần, nhớ giá thấp nhất đã gặp.',
      'Tại mỗi ngày, lợi nhuận = giá hôm nay − giá thấp nhất trước đó; giữ giá trị lớn nhất.',
    ],
    solution: 'function maxProfit(prices) {\n  let min = Infinity, best = 0;\n  for (const p of prices) {\n    if (p < min) min = p;\n    else if (p - min > best) best = p - min;\n  }\n  return best;\n}',
    explain: 'Tham lam một lượt: luôn giả định mua ở giá thấp nhất từng thấy, cập nhật lãi lớn nhất khi bán hôm nay. O(n) thời gian, O(1) bộ nhớ.',
  },
  {
    id: 'majority-element', title: 'Phần tử đa số (Boyer-Moore)',
    topic: 'Tham lam', difficulty: 'Trung bình', fnName: 'majorityElement',
    prompt: 'Tìm phần tử xuất hiện **nhiều hơn ⌊n/2⌋ lần** (luôn tồn tại). Ví dụ `[2,2,1,1,1,2,2]` → `2`.',
    starter: 'function majorityElement(nums) {\n\n}',
    tests: [
      { args: [[3, 2, 3]], expected: 3 },
      { args: [[2, 2, 1, 1, 1, 2, 2]], expected: 2 },
      { args: [[1]], expected: 1 },
      { args: [[5, 5, 5, 4]], expected: 5 },
    ],
    hints: [
      'Cách đơn giản: đếm bằng Map rồi tìm cái > n/2.',
      'Tối ưu O(1) bộ nhớ: thuật toán bỏ phiếu Boyer-Moore — giữ một "ứng viên" và một bộ đếm.',
    ],
    solution: 'function majorityElement(nums) {\n  let count = 0, cand = null;\n  for (const x of nums) {\n    if (count === 0) cand = x;\n    count += (x === cand) ? 1 : -1;\n  }\n  return cand;\n}',
    explain: 'Boyer-Moore: vì phần tử đa số chiếm > một nửa, các phần tử khác không thể triệt tiêu hết nó. O(n) thời gian, O(1) bộ nhớ.',
  },
  {
    id: 'product-except-self', title: 'Tích mảng trừ chính nó (không chia)',
    topic: 'Mảng', difficulty: 'Khó', fnName: 'productExceptSelf',
    prompt: 'Trả về mảng `out` với `out[i]` = tích **tất cả phần tử trừ nums[i]**, KHÔNG được dùng phép chia. Ví dụ `[1,2,3,4]` → `[24,12,8,6]`.',
    starter: 'function productExceptSelf(nums) {\n\n}',
    tests: [
      { args: [[1, 2, 3, 4]], expected: [24, 12, 8, 6] },
      { args: [[2, 3]], expected: [3, 2] },
      { args: [[5, 0, 2]], expected: [0, 10, 0] },
      { args: [[1, 1, 1]], expected: [1, 1, 1] },
    ],
    hints: [
      'out[i] = (tích mọi phần tử bên trái i) × (tích mọi phần tử bên phải i).',
      'Quét trái→phải tích lũy prefix, rồi phải→trái tích lũy suffix.',
    ],
    solution: 'function productExceptSelf(nums) {\n  const n = nums.length;\n  const out = new Array(n).fill(1);\n  let pre = 1;\n  for (let i = 0; i < n; i++) { out[i] = pre; pre *= nums[i]; }\n  let suf = 1;\n  for (let i = n - 1; i >= 0; i--) { out[i] *= suf; suf *= nums[i]; }\n  return out;\n}',
    explain: 'Hai lượt: lượt đầu lưu tích bên trái mỗi vị trí, lượt sau nhân thêm tích bên phải. Tránh phép chia (xử lý được cả số 0). O(n) thời gian, O(1) bộ nhớ phụ.',
  },
  {
    id: 'first-unique-char', title: 'Ký tự đầu tiên không lặp',
    topic: 'Hash Map', difficulty: 'Dễ', fnName: 'firstUniqChar',
    prompt: 'Trả về **chỉ số** của ký tự đầu tiên không lặp lại trong chuỗi `s`; nếu không có, trả về `-1`. Ví dụ `"leetcode"` → `0`, `"loveleetcode"` → `2`, `"aabb"` → `-1`.',
    starter: 'function firstUniqChar(s) {\n\n}',
    tests: [
      { args: ['leetcode'], expected: 0 },
      { args: ['loveleetcode'], expected: 2 },
      { args: ['aabb'], expected: -1 },
      { args: [''], expected: -1 },
    ],
    hints: [
      'Đếm số lần xuất hiện của từng ký tự bằng Map.',
      'Duyệt lại chuỗi, trả về chỉ số đầu tiên có count === 1.',
    ],
    solution: 'function firstUniqChar(s) {\n  const cnt = {};\n  for (const ch of s) cnt[ch] = (cnt[ch] || 0) + 1;\n  for (let i = 0; i < s.length; i++) {\n    if (cnt[s[i]] === 1) return i;\n  }\n  return -1;\n}',
    explain: 'Một lượt đếm tần suất, một lượt tìm ký tự count===1 đầu tiên. O(n) thời gian, O(k) bộ nhớ với k là số ký tự khác nhau.',
  },
  {
    id: 'longest-common-prefix', title: 'Tiền tố chung dài nhất',
    topic: 'Chuỗi', difficulty: 'Dễ', fnName: 'longestCommonPrefix',
    prompt: 'Tìm chuỗi **tiền tố chung dài nhất** của mảng chuỗi `strs`; nếu không có trả về `""`. Ví dụ `["flower","flow","flight"]` → `"fl"`, `["dog","car"]` → `""`.',
    starter: 'function longestCommonPrefix(strs) {\n\n}',
    tests: [
      { args: [['flower', 'flow', 'flight']], expected: 'fl' },
      { args: [['dog', 'racecar', 'car']], expected: '' },
      { args: [['interview', 'interval', 'internal']], expected: 'inter' },
      { args: [['alone']], expected: 'alone' },
    ],
    hints: [
      'Lấy chuỗi đầu làm chuẩn, cắt dần tiền tố cho tới khi mọi chuỗi đều bắt đầu bằng nó.',
      'Hoặc so sánh ký tự theo từng cột cho tất cả chuỗi.',
    ],
    solution: 'function longestCommonPrefix(strs) {\n  if (!strs.length) return "";\n  let pre = strs[0];\n  for (let i = 1; i < strs.length; i++) {\n    while (strs[i].indexOf(pre) !== 0) {\n      pre = pre.slice(0, -1);\n      if (!pre) return "";\n    }\n  }\n  return pre;\n}',
    explain: 'Bắt đầu với chuỗi đầu làm tiền tố, rút ngắn dần đến khi mọi chuỗi đều khớp tiền tố. O(S) với S là tổng số ký tự.',
  },
  {
    id: 'roman-to-int', title: 'Chuyển số La Mã sang số nguyên',
    topic: 'Hash Map', difficulty: 'Trung bình', fnName: 'romanToInt',
    prompt: 'Chuyển chuỗi số La Mã (I,V,X,L,C,D,M) sang số nguyên. Khi ký tự nhỏ đứng trước ký tự lớn thì TRỪ (IV=4, IX=9, XL=40). Ví dụ `"MCMXCIV"` → `1994`, `"LVIII"` → `58`.',
    starter: 'function romanToInt(s) {\n\n}',
    tests: [
      { args: ['III'], expected: 3 },
      { args: ['IV'], expected: 4 },
      { args: ['LVIII'], expected: 58 },
      { args: ['MCMXCIV'], expected: 1994 },
    ],
    hints: [
      'Map mỗi ký tự sang giá trị: I=1, V=5, X=10, L=50, C=100, D=500, M=1000.',
      'Nếu giá trị hiện tại nhỏ hơn giá trị KẾ TIẾP thì trừ, ngược lại cộng.',
    ],
    solution: 'function romanToInt(s) {\n  const v = { I: 1, V: 5, X: 10, L: 50, C: 100, D: 500, M: 1000 };\n  let total = 0;\n  for (let i = 0; i < s.length; i++) {\n    const cur = v[s[i]], next = v[s[i + 1]] || 0;\n    total += cur < next ? -cur : cur;\n  }\n  return total;\n}',
    explain: 'Quét trái→phải: nếu ký tự hiện tại nhỏ hơn ký tự sau, nó mang dấu trừ (mẫu IV, IX). Ngược lại cộng bình thường. O(n).',
  },
  {
    id: 'rotate-array', title: 'Xoay mảng phải k bước',
    topic: 'Mảng', difficulty: 'Trung bình', fnName: 'rotate',
    prompt: 'Xoay mảng `nums` sang **phải** `k` bước và trả về mảng kết quả. Ví dụ `([1,2,3,4,5,6,7], 3)` → `[5,6,7,1,2,3,4]`.',
    starter: 'function rotate(nums, k) {\n\n}',
    tests: [
      { args: [[1, 2, 3, 4, 5, 6, 7], 3], expected: [5, 6, 7, 1, 2, 3, 4] },
      { args: [[-1, -100, 3, 99], 2], expected: [3, 99, -1, -100] },
      { args: [[1, 2], 3], expected: [2, 1] },
      { args: [[1], 0], expected: [1] },
    ],
    hints: [
      'k có thể lớn hơn độ dài mảng → lấy k = k % n.',
      'Phần tử cuối cùng (n-k phần tử) chuyển lên đầu; nối với phần đầu còn lại.',
    ],
    solution: 'function rotate(nums, k) {\n  const n = nums.length;\n  if (n === 0) return nums;\n  k %= n;\n  return nums.slice(n - k).concat(nums.slice(0, n - k));\n}',
    explain: 'Xoay phải k bước = lấy k phần tử cuối đưa lên đầu. Nhớ k %= n để xử lý k lớn. Bản phỏng vấn thường yêu cầu xoay TẠI CHỖ bằng kỹ thuật đảo 3 lần.',
  },
  {
    id: 'title-case', title: 'Viết hoa chữ đầu mỗi từ',
    topic: 'Chuỗi', difficulty: 'Dễ', fnName: 'titleCase',
    prompt: 'Viết hoa chữ cái đầu của mỗi từ, các chữ còn lại viết thường. Các từ cách nhau bởi một dấu cách. Ví dụ `"hello WORLD"` → `"Hello World"`.',
    starter: 'function titleCase(str) {\n\n}',
    tests: [
      { args: ['hello world'], expected: 'Hello World' },
      { args: ['HELLO WORLD'], expected: 'Hello World' },
      { args: ['node js interview'], expected: 'Node Js Interview' },
      { args: ['a'], expected: 'A' },
    ],
    hints: [
      'Tách chuỗi theo dấu cách thành mảng từ.',
      'Với mỗi từ: viết hoa ký tự đầu + viết thường phần còn lại, rồi nối lại bằng dấu cách.',
    ],
    solution: 'function titleCase(str) {\n  return str\n    .split(" ")\n    .map(w => w ? w[0].toUpperCase() + w.slice(1).toLowerCase() : w)\n    .join(" ");\n}',
    explain: 'split → map (chuẩn hoá hoa/thường từng từ) → join. Lưu ý xử lý từ rỗng để không lỗi khi có nhiều dấu cách.',
  },
  {
    id: 'gcd', title: 'Ước chung lớn nhất (Euclid)',
    topic: 'Toán', difficulty: 'Dễ', fnName: 'gcd',
    prompt: 'Tìm **ước chung lớn nhất** của hai số nguyên dương `a`, `b` bằng thuật toán Euclid. Ví dụ `gcd(12, 18)` → `6`, `gcd(7, 13)` → `1`.',
    starter: 'function gcd(a, b) {\n\n}',
    tests: [
      { args: [12, 18], expected: 6 },
      { args: [7, 13], expected: 1 },
      { args: [100, 10], expected: 10 },
      { args: [17, 17], expected: 17 },
    ],
    hints: [
      'Thuật toán Euclid: gcd(a, b) = gcd(b, a mod b).',
      'Dừng khi b = 0, khi đó a chính là kết quả.',
    ],
    solution: 'function gcd(a, b) {\n  while (b !== 0) {\n    [a, b] = [b, a % b];\n  }\n  return a;\n}',
    explain: 'Euclid: thay (a,b) bằng (b, a mod b) cho tới khi b=0. Cực nhanh — O(log(min(a,b))). Nền tảng cho rút gọn phân số, số học mô-đun.',
  },
];
