/**
 * Ngân hàng đề LUYỆN SYSTEM DESIGN (tab 🏛️ Thiết kế hệ thống).
 * Mỗi đề: kịch bản + gợi ý làm rõ + rubric chấm theo khung 5 bước + key points để đối chiếu/AI chấm.
 * Nguồn: system-design-scenarios/ (10 bài kinh điển) + 2 đề nền tảng (URL shortener, rate limiter).
 *
 * rubric[].k    : khóa tiêu chí (ổn định để lưu trạng thái tick)
 * rubric[].w    : trọng số (tổng không cần =100, code tự chuẩn hóa)
 * difficulty    : 'Dễ' | 'TB' | 'Khó'
 */
window.DESIGN_DRILLS = [
  {
    id: 'url-shortener',
    title: 'Rút gọn URL (TinyURL / bit.ly)',
    company: 'Phổ biến · khởi động',
    difficulty: 'Dễ',
    scenario: 'Thiết kế dịch vụ rút gọn URL: nhận URL dài trả về URL ngắn, truy cập URL ngắn thì redirect 301/302 về URL gốc. Quy mô ~100M link mới/tháng, đọc gấp ~100 lần ghi.',
    focus: [
      'Read-heavy cực mạnh (redirect) — tối ưu đọc là trọng tâm',
      'Sinh mã ngắn: counter+base62 vs hash vs random — chống trùng',
      'Có cần custom alias, expiry, analytics không?',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ yêu cầu (functional + non-functional, custom alias/expiry, tỉ lệ đọc/ghi)', hint: 'Hỏi: cho phép alias tùy chọn? TTL? đếm lượt click? độ dài mã?', w: 12 },
      { k: 'est', label: 'Ước lượng (QPS đọc/ghi, dung lượng, băng thông, số ký tự mã cần)', hint: '100M/tháng ≈ 40 ghi/s; đọc ~4000/s. base62^7 ≈ 3.5 nghìn tỉ mã.', w: 14 },
      { k: 'api', label: 'API: POST /shorten, GET /{code} (redirect)', hint: 'Nêu mã trạng thái 301 vs 302 và hệ quả cache/analytics.', w: 12 },
      { k: 'gen', label: 'Chiến lược sinh mã ngắn + chống trùng', hint: 'Counter→base62 (đơn điệu, đoán được) vs hash(MD5)+cắt+xử lý va chạm vs KGS (key generation service) cấp sẵn.', w: 18 },
      { k: 'data', label: 'Data model + chọn storage', hint: 'Bảng (code PK, long_url, created, expiry). NoSQL key-value hợp vì truy cập theo key.', w: 12 },
      { k: 'scale', label: 'Tối ưu đọc: cache + CDN + sharding theo code', hint: 'Cache-aside Redis cho hot link; 301 cho phép browser/CDN cache; shard theo hash(code).', w: 20 },
      { k: 'trade', label: 'Trade-offs & bottleneck (301 vs 302, hot key, single point KGS)', hint: '301 cache mạnh nhưng mất analytics; 302 đếm được click nhưng tải gốc cao.', w: 12 },
    ],
    keyPoints: [
      'Sinh mã: dùng counter phân tán (Redis INCR/Snowflake) rồi base62, hoặc KGS cấp sẵn key đã kiểm trùng để tránh va chạm runtime.',
      'Đọc gấp 100× ghi → cache-aside Redis + cho CDN/browser cache bằng 301 với link bất biến.',
      'Storage key-value (DynamoDB/Cassandra) phù hợp vì luôn tra theo code; shard theo hash(code) để phân bố đều.',
      '301 (permanent) cache tốt nhưng không đếm được click; 302 (temporary) đếm được nhưng mọi request về server.',
      'Bottleneck thường là hot link viral → cache nhiều tầng; KGS là single point → cấp key theo batch, có dự phòng.',
      'Custom alias cần check unique riêng; expiry chạy bằng TTL của store hoặc job dọn nền.',
    ],
  },
  {
    id: 'rate-limiter',
    title: 'Distributed Rate Limiter',
    company: 'Phổ biến · nền tảng',
    difficulty: 'TB',
    scenario: 'Thiết kế rate limiter cho API gateway: giới hạn mỗi user/IP/API key tối đa N request trong cửa sổ thời gian, hoạt động đúng khi có nhiều instance gateway sau load balancer.',
    focus: [
      'Thuật toán: fixed/sliding window, token bucket, leaky bucket',
      'State chia sẻ giữa nhiều node → Redis tập trung vs local + đồng bộ',
      'Trả 429 + header Retry-After; fail-open hay fail-closed khi Redis chết?',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: giới hạn theo gì (user/IP/key), đơn vị, độ chính xác cần, hành vi khi vượt', hint: 'Hỏi: cho phép burst? giới hạn theo endpoint? đồng bộ tuyệt đối hay xấp xỉ được?', w: 12 },
      { k: 'algo', label: 'Chọn & so sánh thuật toán', hint: 'Fixed window (burst ở ranh giới), sliding log (chính xác, tốn RAM), sliding counter (xấp xỉ), token bucket (cho burst có kiểm soát).', w: 22 },
      { k: 'dist', label: 'State phân tán: Redis tập trung (INCR+EXPIRE / Lua atomic)', hint: 'Lua script gộp check+incr nguyên tử tránh race; hoặc sorted set cho sliding log.', w: 20 },
      { k: 'api', label: 'Giao diện: middleware, trả 429 + Retry-After + X-RateLimit-*', hint: 'Header cho client biết còn bao nhiêu quota và khi nào reset.', w: 12 },
      { k: 'fail', label: 'Xử lý Redis chết: fail-open vs fail-closed; latency thêm', hint: 'Fail-open (cho qua) ưu tiên sẵn sàng; fail-closed (chặn) ưu tiên bảo vệ. Có local cache đệm.', w: 14 },
      { k: 'scale', label: 'Mở rộng: sharding theo key, local token bucket + đồng bộ định kỳ', hint: 'Giảm round-trip Redis bằng cách cấp phát quota theo lô cho từng node.', w: 12 },
      { k: 'trade', label: 'Trade-offs (chính xác vs throughput, RAM vs độ mượt)', hint: 'Sliding log chính xác nhất nhưng tốn bộ nhớ; counter xấp xỉ đủ dùng đa số.', w: 8 },
    ],
    keyPoints: [
      'Token bucket là lựa chọn mặc định tốt: cho burst tới dung lượng bucket, bù token đều theo thời gian.',
      'State tập trung ở Redis dùng Lua script để check-and-increment nguyên tử, tránh race giữa các node.',
      'Sliding window log (sorted set theo timestamp) chính xác nhất nhưng tốn RAM; sliding window counter nội suy là cân bằng tốt.',
      'Khi Redis sự cố: quyết định fail-open (ưu tiên trải nghiệm) hay fail-closed (ưu tiên bảo vệ backend) tùy nghiệp vụ.',
      'Trả về 429 kèm Retry-After và header X-RateLimit-Remaining/Reset để client tự điều tiết.',
      'Giảm tải Redis: mỗi node giữ token bucket cục bộ, xin quota theo lô và đồng bộ định kỳ (đổi chính xác lấy throughput).',
    ],
  },
  {
    id: 'news-feed',
    title: 'News Feed (Facebook / Instagram)',
    company: 'Meta · kinh điển',
    difficulty: 'Khó',
    scenario: 'Thiết kế news feed: user xem dòng bài của những người họ follow, sắp xếp theo thời gian/độ liên quan, cập nhật gần thời gian thực. ~500M DAU, người nổi tiếng có hàng chục triệu follower.',
    focus: [
      'Fan-out on write (push) vs fan-out on read (pull) — và mô hình lai',
      'Celebrity problem: fan-out cho người nổi tiếng cực đắt',
      'Ranking, phân trang theo cursor, cache feed',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: feed theo thời gian hay ranking, độ trễ chấp nhận, follow 1 chiều/2 chiều', hint: 'Hỏi: realtime tới đâu? feed cá nhân hóa? giới hạn số bài?', w: 10 },
      { k: 'est', label: 'Ước lượng (DAU, bài/ngày, đọc feed/ngày, dung lượng)', hint: '500M DAU, đọc gấp nhiều lần ghi → read-heavy.', w: 12 },
      { k: 'api', label: 'API: getFeed(cursor), post(content), follow(userId)', hint: 'Phân trang bằng cursor/keyset, không offset.', w: 10 },
      { k: 'fanout', label: 'Fan-out: push (write) vs pull (read) + mô hình lai', hint: 'Push: ghi vào feed cache mỗi follower (đọc nhanh, ghi đắt). Pull: gộp lúc đọc (ghi rẻ, đọc chậm).', w: 22 },
      { k: 'celeb', label: 'Xử lý celebrity / hot user', hint: 'Người nổi tiếng KHÔNG fan-out; follower của họ pull riêng rồi merge với feed đã push.', w: 16 },
      { k: 'data', label: 'Data model + storage (feed cache Redis, post store, graph follow)', hint: 'Feed cache là list id bài trong Redis; bài để ở DB/columnar.', w: 12 },
      { k: 'trade', label: 'Trade-offs & bottleneck (độ trễ fan-out, RAM feed cache, ranking)', hint: 'Push tốn ghi và RAM; pull tốn đọc. Lai để cân bằng.', w: 18 },
    ],
    keyPoints: [
      'Mô hình lai: fan-out-on-write cho user thường (đọc feed nhanh), fan-out-on-read cho celebrity (tránh ghi hàng chục triệu bản).',
      'Feed cache lưu danh sách post-id trong Redis (list/sorted set), hydrate nội dung bài lúc đọc từ post store.',
      'Phân trang bằng cursor/keyset theo timestamp hoặc score, không dùng OFFSET (chậm ở trang sâu).',
      'Ranking: bắt đầu bằng reverse-chronological, nâng cấp dần bằng điểm (engagement, recency, affinity).',
      'Bottleneck là fan-out write amplification và RAM của feed cache → giới hạn độ dài feed cache, TTL, lazy rebuild.',
      'Graph follow tách riêng (adjacency list/graph store); đọc danh sách following để pull phần celebrity.',
    ],
  },
  {
    id: 'chat',
    title: 'Chat App (Messenger / WhatsApp)',
    company: 'Meta · kinh điển',
    difficulty: 'Khó',
    scenario: 'Thiết kế ứng dụng nhắn tin realtime: 1-1 và group, trạng thái online, đã gửi/đã nhận/đã đọc, lưu lịch sử, đẩy thông báo khi offline. Hàng trăm triệu user.',
    focus: [
      'Kết nối lâu dài: WebSocket + connection/session service',
      'Đảm bảo gửi: at-least-once + dedup, thứ tự tin nhắn',
      'Lưu trữ tin nhắn (đọc theo conversation), presence',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: 1-1 vs group, kích thước group, online status, đã đọc, đa thiết bị', hint: 'Hỏi: cần E2E encryption? lịch sử bao lâu? gửi media?', w: 10 },
      { k: 'conn', label: 'Tầng kết nối realtime: WebSocket + gateway giữ session', hint: 'Map userId → server đang giữ kết nối (qua Redis/registry); LB sticky/connection-aware.', w: 20 },
      { k: 'deliver', label: 'Đảm bảo gửi + thứ tự + dedup', hint: 'Ack từng tin; message id + sequence; at-least-once rồi khử trùng phía client.', w: 18 },
      { k: 'data', label: 'Lưu trữ tin nhắn (đọc theo conversation, phân vùng)', hint: 'Cassandra/HBase: partition theo conversation_id, clustering theo time → đọc dải nhanh.', w: 14 },
      { k: 'offline', label: 'Offline: hàng đợi tin + push notification', hint: 'User offline → lưu vào inbox/queue, gửi khi reconnect; APNs/FCM cho push.', w: 12 },
      { k: 'presence', label: 'Presence (online/typing) + group fan-out', hint: 'Heartbeat + TTL trong Redis; group lớn fan-out theo nhóm thành viên.', w: 12 },
      { k: 'trade', label: 'Trade-offs & bottleneck (số kết nối/server, thứ tự, E2E)', hint: 'Hàng triệu kết nối cần nhiều gateway; thứ tự toàn cục khó → thứ tự theo conversation.', w: 14 },
    ],
    keyPoints: [
      'WebSocket bền cho realtime; một registry (Redis) map user→gateway server để định tuyến tin tới đúng kết nối.',
      'Mỗi tin có message id + sequence theo conversation để khử trùng và sắp thứ tự; gửi at-least-once + ack.',
      'Lưu tin trong store phân vùng theo conversation_id, clustering theo thời gian (Cassandra) để đọc lịch sử theo dải nhanh.',
      'User offline: tin vào hàng đợi/inbox, đẩy push qua APNs/FCM, đồng bộ khi thiết bị reconnect.',
      'Presence bằng heartbeat + TTL Redis; typing/seen là event nhẹ fan-out tới thành viên hội thoại.',
      'Bottleneck: số kết nối đồng thời mỗi gateway và fan-out group lớn → scale ngang gateway, giới hạn kích thước group.',
    ],
  },
  {
    id: 'flash-sale',
    title: 'Đặt vé / Flash sale (Ticketmaster / Shopee)',
    company: 'E-commerce · kinh điển',
    difficulty: 'Khó',
    scenario: 'Thiết kế hệ thống bán vé/flash sale: lượng tồn có hạn, hàng trăm nghìn người tranh mua cùng lúc, KHÔNG được oversell, giữ chỗ tạm khi đang thanh toán.',
    focus: [
      'Chống oversell dưới tải cực cao (race condition trên tồn kho)',
      'Reservation/hold có timeout, hoàn lại khi không thanh toán',
      'Hàng đợi ảo (virtual waiting room), chống bot',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: số lượng tồn, hold timeout, công bằng (FIFO?), chống bot', hint: 'Hỏi: giữ chỗ bao lâu? cho phép chờ? một người mua tối đa?', w: 10 },
      { k: 'oversell', label: 'Chống oversell (atomic decrement / DB constraint / Redis)', hint: 'Redis DECR atomic, hoặc UPDATE ... WHERE stock>0 (kiểm hàng bị ảnh hưởng), hoặc optimistic lock theo version.', w: 24 },
      { k: 'hold', label: 'Reservation có TTL + hoàn kho khi hết hạn', hint: 'Giữ chỗ = trừ tạm + TTL; thanh toán xong mới chốt; hết hạn job/TTL hoàn kho.', w: 18 },
      { k: 'queue', label: 'Hàng đợi ảo / điều tiết tải đỉnh', hint: 'Virtual waiting room cấp token theo nhịp; chỉ N người vào trang mua cùng lúc.', w: 14 },
      { k: 'data', label: 'Data model: inventory, reservation, order; tính nhất quán mạnh', hint: 'Tồn kho cần strong consistency (CP) — thà từ chối còn hơn bán quá.', w: 12 },
      { k: 'scale', label: 'Mở rộng + chống bot (rate limit, captcha, idempotency)', hint: 'Idempotency key chống double-buy khi user bấm lại/timeout retry.', w: 10 },
      { k: 'trade', label: 'Trade-offs & bottleneck (hotspot 1 sự kiện, CP vs AP)', hint: 'Tất cả dồn vào 1 inventory key → hotspot; ưu tiên đúng (CP) hơn sẵn sàng.', w: 12 },
    ],
    keyPoints: [
      'Chống oversell bằng thao tác nguyên tử: Redis DECR (chặn khi <0) hoặc SQL UPDATE stock=stock-1 WHERE stock>0 và kiểm số hàng bị ảnh hưởng.',
      'Giữ chỗ tạm (reservation) có TTL: trừ kho ngay, thanh toán xong mới chốt order, hết hạn thì TTL/job hoàn kho.',
      'Hàng đợi ảo (waiting room) cấp lượt vào theo nhịp để bảo vệ backend khỏi đỉnh tải tức thời.',
      'Tồn kho yêu cầu strong consistency (CP) — chấp nhận từ chối/chậm chứ không bán quá.',
      'Idempotency key cho thao tác mua để retry/double-click không tạo 2 đơn; rate limit + captcha chống bot.',
      'Bottleneck là hotspot trên một inventory key của sự kiện hot → có thể chia nhỏ tồn kho thành nhiều shard cộng dồn.',
    ],
  },
  {
    id: 'autocomplete',
    title: 'Search Autocomplete (Google suggest)',
    company: 'Google · kinh điển',
    difficulty: 'TB',
    scenario: 'Thiết kế gợi ý từ khóa khi gõ: trả top-k gợi ý phổ biến nhất theo tiền tố, độ trễ < 100ms, cập nhật xu hướng theo thời gian.',
    focus: [
      'Cấu trúc Trie + lưu sẵn top-k tại mỗi node',
      'Read-heavy cực mạnh, ưu tiên latency → cache, precompute',
      'Cập nhật tần suất offline vs near-realtime',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: số gợi ý (k), độ trễ, cá nhân hóa, đa ngôn ngữ, lọc bậy', hint: 'Hỏi: top mấy? theo personalization? cập nhật trend nhanh tới đâu?', w: 12 },
      { k: 'est', label: 'Ước lượng QPS (mỗi phím gõ là 1 query) + dung lượng trie', hint: 'Gõ là gọi liên tục → QPS cực cao, phải debounce + cache.', w: 12 },
      { k: 'trie', label: 'Trie + precompute top-k tại mỗi node', hint: 'Mỗi node trie lưu sẵn k gợi ý hàng đầu của tiền tố đó → tra O(độ dài tiền tố).', w: 24 },
      { k: 'update', label: 'Cập nhật tần suất: thu log → tính offline → nạp lại trie', hint: 'Aggregate query log (batch/stream) cập nhật count, rebuild/merge trie định kỳ.', w: 16 },
      { k: 'scale', label: 'Mở rộng: shard trie theo tiền tố + cache + CDN edge', hint: 'Cache kết quả theo prefix; phân vùng trie theo chữ cái đầu.', w: 14 },
      { k: 'data', label: 'Lưu trữ + phục vụ (trie trong RAM, snapshot bền)', hint: 'Trie phục vụ từ memory; snapshot ra storage để khởi động lại.', w: 10 },
      { k: 'trade', label: 'Trade-offs (độ tươi của trend vs chi phí rebuild, RAM)', hint: 'Rebuild thường xuyên = tươi nhưng tốn; precompute = nhanh nhưng trễ cập nhật.', w: 12 },
    ],
    keyPoints: [
      'Trie với top-k gợi ý precompute tại mỗi node → tra cứu theo tiền tố cực nhanh, không phải quét.',
      'Read-heavy + latency-critical → phục vụ trie từ RAM, cache kết quả theo prefix, đẩy ra edge/CDN.',
      'Debounce phía client để mỗi phím gõ không tạo một request riêng, giảm QPS.',
      'Cập nhật tần suất offline: gom query log (batch hoặc stream), tính lại count, rebuild/merge trie định kỳ rồi nạp nóng.',
      'Shard trie theo chữ cái/tiền tố đầu để scale ngang và giảm kích thước mỗi phân vùng.',
      'Trade-off độ tươi của trend vs chi phí rebuild; thêm lọc nội dung nhạy cảm và hỗ trợ đa ngôn ngữ.',
    ],
  },
  {
    id: 'video-streaming',
    title: 'Video Streaming (YouTube / TikTok)',
    company: 'Video · kinh điển',
    difficulty: 'Khó',
    scenario: 'Thiết kế nền tảng xem video: upload, transcode nhiều độ phân giải, phát mượt toàn cầu (adaptive bitrate), metadata + lượt xem. Lưu trữ petabyte, người dùng toàn cầu.',
    focus: [
      'Pipeline upload → transcode (async, queue) → lưu segment',
      'CDN + adaptive bitrate streaming (HLS/DASH)',
      'Tách metadata service và blob storage; đếm view',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: upload/playback, độ phân giải, toàn cầu, đếm view, đề xuất', hint: 'Hỏi: live hay VOD? độ trễ phát? bản quyền?', w: 10 },
      { k: 'upload', label: 'Pipeline upload + transcode bất đồng bộ', hint: 'Upload lên blob → đẩy job vào queue → worker transcode ra nhiều bitrate/segment.', w: 20 },
      { k: 'cdn', label: 'Phân phối: CDN + adaptive bitrate (HLS/DASH)', hint: 'Chia video thành segment ngắn; client tự chọn bitrate theo băng thông; cache ở edge.', w: 22 },
      { k: 'storage', label: 'Lưu trữ blob (object storage) + metadata DB tách biệt', hint: 'Video ở S3/object store; metadata (title, owner, view) ở DB riêng.', w: 14 },
      { k: 'view', label: 'Đếm view/analytics quy mô lớn (xấp xỉ, async)', hint: 'Đếm view qua stream + aggregate; chấp nhận eventual, dùng HLL cho unique.', w: 12 },
      { k: 'scale', label: 'Mở rộng toàn cầu + hot video', hint: 'Edge cache cho video hot; transcode scale theo queue depth.', w: 10 },
      { k: 'trade', label: 'Trade-offs & bottleneck (chi phí lưu trữ/transcode, độ trễ phát)', hint: 'Transcode tốn CPU; lưu nhiều bitrate tốn dung lượng; cân theo độ phổ biến.', w: 12 },
    ],
    keyPoints: [
      'Upload tách khỏi transcode: nhận file vào object storage rồi đẩy job vào queue cho fleet worker transcode bất đồng bộ.',
      'Transcode ra nhiều độ phân giải/bitrate, cắt thành segment ngắn cho HLS/DASH adaptive streaming.',
      'CDN là cốt lõi phát toàn cầu: cache segment ở edge gần user, giảm tải gốc và độ trễ.',
      'Tách blob storage (video) khỏi metadata DB (title, owner, view count) — hai đặc tính truy cập rất khác nhau.',
      'Đếm view/analytics ở quy mô lớn dùng pipeline stream + aggregate, chấp nhận eventual; HyperLogLog cho unique viewer.',
      'Bottleneck: chi phí transcode (CPU) và lưu trữ nhiều bitrate → chỉ tạo sẵn bitrate cho video phổ biến, phần còn lại lazy.',
    ],
  },
  {
    id: 'payment',
    title: 'Payment System (Stripe-lite / Ví điện tử)',
    company: 'Fintech · kinh điển',
    difficulty: 'Khó',
    scenario: 'Thiết kế hệ thống thanh toán: trừ tiền người mua, cộng cho người bán, tích hợp cổng ngân hàng/PSP, đảm bảo KHÔNG mất tiền/double charge, đối soát. Tính đúng đắn tiền bạc là tối thượng.',
    focus: [
      'Tính nhất quán mạnh + idempotency cho thao tác tiền',
      'Ledger double-entry, không cập nhật số dư kiểu mutable thuần',
      'Xử lý PSP bất đồng bộ, webhook, đối soát (reconciliation)',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: loại giao dịch, tiền tệ, PSP, yêu cầu nhất quán/audit', hint: 'Hỏi: hoàn tiền? đa tiền tệ? mức độ chịu lỗi? audit/compliance?', w: 10 },
      { k: 'idem', label: 'Idempotency cho mọi thao tác tiền', hint: 'Idempotency key duy nhất/giao dịch; lưu kết quả theo key → retry không charge 2 lần.', w: 22 },
      { k: 'ledger', label: 'Ledger double-entry (ghi sổ thay vì sửa số dư)', hint: 'Mỗi giao dịch là cặp bút toán nợ/có; số dư = tổng hợp; bất biến, audit được.', w: 20 },
      { k: 'psp', label: 'Tích hợp PSP bất đồng bộ + webhook + state machine', hint: 'Trạng thái: pending→authorized→captured/failed; webhook cập nhật; xử lý trùng webhook.', w: 16 },
      { k: 'consistency', label: 'Nhất quán mạnh + xử lý lỗi/timeout PSP', hint: 'Dùng transaction/saga; timeout PSP → trạng thái unknown phải đối soát, không đoán.', w: 14 },
      { k: 'recon', label: 'Đối soát (reconciliation) định kỳ với PSP/ngân hàng', hint: 'So sổ nội bộ với báo cáo PSP, phát hiện lệch, sửa có kiểm soát.', w: 10 },
      { k: 'trade', label: 'Trade-offs & bottleneck (đúng đắn vs throughput, hotspot account)', hint: 'Ưu tiên correctness tuyệt đối; tài khoản nóng (merchant lớn) cần xử lý khéo.', w: 8 },
    ],
    keyPoints: [
      'Idempotency key bắt buộc cho mọi thao tác tiền: lưu kết quả theo key, retry/timeout không bao giờ charge hai lần.',
      'Dùng ledger double-entry (bút toán nợ/có bất biến) thay vì sửa trực tiếp số dư — audit được và không mất dấu.',
      'Tích hợp PSP qua state machine (pending→authorized→captured/failed) + webhook; webhook phải idempotent vì có thể trùng.',
      'Tiền bạc cần strong consistency: transaction/saga rõ ràng; PSP timeout để trạng thái "unknown" và để reconciliation quyết, không tự đoán.',
      'Đối soát định kỳ giữa sổ nội bộ và báo cáo PSP/ngân hàng để phát hiện và sửa lệch.',
      'Bottleneck là tài khoản nóng (merchant lớn) và yêu cầu correctness — ưu tiên đúng hơn nhanh, tối ưu sau bằng batching an toàn.',
    ],
  },
  {
    id: 'job-scheduler',
    title: 'Distributed Job Scheduler (Airflow-lite / Delayed jobs)',
    company: 'Hạ tầng · kinh điển',
    difficulty: 'Khó',
    scenario: 'Thiết kế hệ thống lập lịch job phân tán: chạy job theo cron hoặc delay, thử lại khi lỗi, không chạy trùng, mở rộng tới hàng triệu job, một số job có phụ thuộc (DAG).',
    focus: [
      'Đảm bảo chạy đúng-một-lần (xấp xỉ) + idempotent worker',
      'Hàng đợi có độ trễ (delay queue) + leasing/locking job',
      'Phụ thuộc job (DAG), retry/backoff, theo dõi trạng thái',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: cron vs delay, độ chính xác giờ, retry, DAG, quy mô', hint: 'Hỏi: trễ chấp nhận bao nhiêu? exactly-once cần thế nào? job lâu hay ngắn?', w: 10 },
      { k: 'store', label: 'Lưu job + truy vấn "job đến hạn" hiệu quả', hint: 'Bảng job (next_run index) hoặc sorted set theo thời gian; poll job due.', w: 16 },
      { k: 'dispatch', label: 'Điều phối + leasing/lock chống chạy trùng', hint: 'Worker "claim" job bằng lock/lease có TTL (SELECT FOR UPDATE SKIP LOCKED / atomic claim).', w: 22 },
      { k: 'exec', label: 'Đảm bảo thực thi: at-least-once + idempotent + visibility timeout', hint: 'Lease hết hạn (worker chết) → job được giao lại; worker idempotent để chạy lại an toàn.', w: 18 },
      { k: 'retry', label: 'Retry/backoff + DLQ cho job lỗi mãi', hint: 'Exponential backoff + jitter, giới hạn lần, quá ngưỡng đẩy DLQ.', w: 12 },
      { k: 'dag', label: 'Phụ thuộc job (DAG) + trạng thái', hint: 'Chỉ chạy khi node cha hoàn thành; lưu trạng thái từng node.', w: 10 },
      { k: 'trade', label: 'Trade-offs & bottleneck (độ chính xác giờ vs tải poll, thundering herd)', hint: 'Nhiều job cùng giờ → thundering herd; rải jitter, phân vùng theo thời gian.', w: 12 },
    ],
    keyPoints: [
      'Lưu job với chỉ mục theo thời gian chạy kế tiếp (sorted set / index next_run); worker poll các job đến hạn.',
      'Chống chạy trùng bằng leasing/lock có TTL: claim nguyên tử (SELECT ... FOR UPDATE SKIP LOCKED hoặc atomic update trạng thái).',
      'Đảm bảo at-least-once + visibility timeout: worker chết giữa chừng thì lease hết hạn và job được giao lại; worker phải idempotent.',
      'Retry với exponential backoff + jitter, giới hạn số lần; lỗi mãi đẩy sang DLQ để xử lý tay.',
      'Phụ thuộc DAG: chỉ enqueue job con khi mọi cha hoàn thành; lưu trạng thái từng node để khôi phục.',
      'Bottleneck: nhiều job cùng mốc giờ gây thundering herd → rải jitter, phân vùng theo thời gian, scale worker theo độ sâu hàng đợi.',
    ],
  },
  {
    id: 'leaderboard',
    title: 'Real-time Leaderboard (Game ranking / Top seller)',
    company: 'Gaming · kinh điển',
    difficulty: 'TB',
    scenario: 'Thiết kế bảng xếp hạng realtime: cập nhật điểm liên tục, truy vấn top-N và thứ hạng của một user tức thì, hàng chục triệu người chơi, có bảng theo ngày/tuần/all-time.',
    focus: [
      'Redis Sorted Set (ZADD/ZREVRANGE/ZRANK) là lõi',
      'Lấy rank của 1 user nhanh; bảng theo khung thời gian',
      'Mở rộng khi quá lớn: shard, xấp xỉ rank',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: top-N, rank cá nhân, khung thời gian, độ chính xác rank', hint: 'Hỏi: cần rank chính xác hay xấp xỉ? bao nhiêu bảng? cập nhật nhanh tới đâu?', w: 12 },
      { k: 'core', label: 'Sorted Set làm lõi (ZADD, ZREVRANGE, ZRANK)', hint: 'Score=điểm; top-N = ZREVRANGE 0 N-1; rank = ZREVRANK O(log n).', w: 24 },
      { k: 'window', label: 'Bảng theo ngày/tuần/all-time', hint: 'Mỗi khung là một sorted set có TTL (vd lb:daily:2026-06-26); all-time không TTL.', w: 14 },
      { k: 'data', label: 'Nguồn điểm bền + đồng bộ với Redis', hint: 'Ghi điểm vào DB bền + cập nhật sorted set; rebuild được từ DB khi mất cache.', w: 14 },
      { k: 'scale', label: 'Mở rộng khi quá lớn (shard theo dải điểm, rank xấp xỉ)', hint: 'Hàng trăm triệu entry → phân vùng; rank chính xác toàn cục đắt → có thể xấp xỉ.', w: 16 },
      { k: 'realtime', label: 'Cập nhật realtime + đọc top nóng (cache top-N)', hint: 'Top-N thay đổi chậm → cache ngắn; cập nhật score là O(log n) rẻ.', w: 10 },
      { k: 'trade', label: 'Trade-offs & bottleneck (RAM sorted set, rank chính xác vs xấp xỉ)', hint: 'Sorted set lớn tốn RAM; rank chính xác toàn cục khó scale ngang.', w: 10 },
    ],
    keyPoints: [
      'Lõi là Redis Sorted Set: ZADD cập nhật điểm, ZREVRANGE lấy top-N, ZREVRANK lấy thứ hạng user — đều O(log n).',
      'Mỗi khung thời gian là một sorted set riêng có TTL (daily/weekly), all-time giữ lâu dài.',
      'Lưu điểm bền ở DB nguồn để rebuild được sorted set khi Redis mất; sorted set là lớp phục vụ nhanh.',
      'Top-N thay đổi tương đối chậm → cache kết quả top ngắn hạn; cập nhật điểm vẫn realtime và rẻ.',
      'Khi cực lớn: shard theo dải điểm hoặc theo region; rank chính xác toàn cục đắt nên có thể chấp nhận rank xấp xỉ.',
      'Bottleneck là RAM của sorted set khổng lồ và chi phí rank chính xác toàn cục → cân giữa chính xác và khả năng mở rộng.',
    ],
  },
  {
    id: 'proximity',
    title: 'Proximity Service (Yelp nearby / Grab tìm tài xế)',
    company: 'Location · kinh điển',
    difficulty: 'Khó',
    scenario: 'Thiết kế dịch vụ tìm đối tượng gần: trả về nhà hàng/tài xế trong bán kính R quanh vị trí user, cập nhật vị trí (với tài xế di chuyển) realtime, độ trễ thấp, quy mô thành phố lớn.',
    focus: [
      'Lập chỉ mục không gian: Geohash / Quadtree / S2 / Redis GEO',
      'Đối tượng tĩnh (nhà hàng) vs động (tài xế cập nhật liên tục)',
      'Truy vấn bán kính hiệu quả + hot area',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: bán kính/top-K, tĩnh hay động, tần suất cập nhật vị trí, độ trễ', hint: 'Hỏi: tìm nhà hàng (tĩnh) hay tài xế (động)? cập nhật vị trí mỗi mấy giây?', w: 12 },
      { k: 'index', label: 'Chỉ mục không gian (Geohash/Quadtree/S2/Redis GEO)', hint: 'Geohash chia ô lưới; quadtree chia thích nghi theo mật độ; Redis GEO (GEOADD/GEOSEARCH).', w: 24 },
      { k: 'query', label: 'Truy vấn bán kính: ô trung tâm + 8 ô lân cận', hint: 'Tìm trong ô chứa điểm và các ô kề để không sót đối tượng gần ranh giới.', w: 16 },
      { k: 'dynamic', label: 'Xử lý đối tượng động (tài xế cập nhật vị trí liên tục)', hint: 'Cập nhật index khi vị trí đổi; ghi nhiều → dùng store ghi nhanh + TTL.', w: 18 },
      { k: 'scale', label: 'Mở rộng + hot area (khu trung tâm đông)', hint: 'Quadtree thích nghi mật độ; shard theo vùng; cache vùng nóng.', w: 12 },
      { k: 'data', label: 'Data model + storage cho vị trí + thông tin đối tượng', hint: 'Index không gian tách khỏi metadata (tên, đánh giá).', w: 8 },
      { k: 'trade', label: 'Trade-offs & bottleneck (độ chi tiết ô vs số ô quét, cập nhật động)', hint: 'Ô lớn = ít ô nhưng nhiều ứng viên; ô nhỏ = nhiều ô. Đối tượng động làm index nóng.', w: 10 },
    ],
    keyPoints: [
      'Dùng chỉ mục không gian: Geohash (ô lưới theo độ dài tiền tố), Quadtree (chia thích nghi theo mật độ), hoặc Redis GEO/S2.',
      'Truy vấn bán kính: tìm trong ô chứa điểm và 8 ô lân cận để không sót đối tượng nằm gần ranh giới ô.',
      'Phân biệt đối tượng tĩnh (nhà hàng — index ít đổi) và động (tài xế — cập nhật vị trí liên tục, ghi nặng).',
      'Đối tượng động: cập nhật index theo mỗi lần báo vị trí, dùng store ghi nhanh + TTL để dọn vị trí cũ.',
      'Hot area (trung tâm thành phố) mật độ cao → quadtree chia nhỏ thích nghi, shard theo vùng, cache vùng nóng.',
      'Trade-off độ chi tiết ô: ô lớn ít ô phải quét nhưng nhiều ứng viên cần lọc; ô nhỏ ngược lại — chọn theo mật độ.',
    ],
  },
  {
    id: 'notification',
    title: 'Hệ thống Thông báo (Notification Service)',
    company: 'Phổ biến · nền tảng',
    difficulty: 'TB',
    scenario: 'Thiết kế dịch vụ gửi thông báo đa kênh (push, email, SMS, in-app) cho nhiều service nội bộ gọi tới. Quy mô hàng chục triệu thông báo/ngày, có loại gấp (OTP) và loại hàng loạt (marketing), cần đáng tin cậy và không gửi trùng.',
    focus: [
      'Nhiều kênh (push/email/SMS/in-app) — trừu tượng hoá provider adapter',
      'Hàng đợi để tách gửi khỏi request gọi đến; ưu tiên OTP vs marketing',
      'Độ tin cậy: retry, DLQ, chống gửi trùng (idempotency/dedup), tôn trọng tuỳ chọn người dùng',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ yêu cầu (kênh nào, mức ưu tiên, đảm bảo gửi, chống trùng, opt-out)', hint: 'Hỏi: at-least-once hay exactly-once? OTP có SLA riêng? có template & đa ngôn ngữ? người dùng tắt kênh được không?', w: 12 },
      { k: 'est', label: 'Ước lượng (QPS gửi, fanout, tỉ lệ theo kênh, dung lượng lưu trạng thái)', hint: '30M/ngày ≈ 350/s trung bình, đỉnh gấp nhiều lần khi gửi chiến dịch → phải đệm bằng queue.', w: 12 },
      { k: 'api', label: 'API: POST /notify (type, recipients, template, data, priority) + quản lý preferences/subscription', hint: 'Tách API nhận yêu cầu (nhanh, chỉ enqueue) khỏi việc gửi thật (async worker).', w: 10 },
      { k: 'arch', label: 'Kiến trúc: queue + worker theo kênh + provider adapter', hint: 'Ingest → validate/dedup → hàng đợi ưu tiên (OTP queue riêng) → worker mỗi kênh → adapter (FCM/APNs, SES/SendGrid, Twilio). Tách hàng đợi theo priority để OTP không kẹt sau batch marketing.', w: 20 },
      { k: 'reliability', label: 'Độ tin cậy: retry có backoff, DLQ, idempotency key chống gửi trùng', hint: 'At-least-once + dedup bằng idempotency key (hash type+recipient+nội dung trong TTL). Provider lỗi → retry exponential backoff, quá số lần → DLQ + cảnh báo.', w: 20 },
      { k: 'data', label: 'Data model: bản ghi notification + trạng thái (queued/sent/failed) + user preferences', hint: 'Lưu trạng thái để truy vết & thử lại; bảng preferences để lọc kênh người dùng đã tắt; in-app dùng store riêng cho badge/đọc-chưa đọc.', w: 12 },
      { k: 'trade', label: 'Trade-offs & bottleneck (rate limit theo provider, ordering, template rendering, fan-out lớn)', hint: 'Tôn trọng rate limit từng provider (token bucket per provider); chiến dịch fan-out lớn → chia batch + scheduler; ordering thường không cần tuyệt đối trừ vài ca.', w: 14 },
    ],
    keyPoints: [
      'Tách API nhận yêu cầu (chỉ validate + enqueue, trả nhanh) khỏi worker gửi thật — chịu tải đỉnh bằng hàng đợi.',
      'Dùng hàng đợi ƯU TIÊN riêng cho loại gấp (OTP) để không kẹt sau chiến dịch marketing hàng loạt.',
      'Provider adapter chung một interface (send) cho FCM/APNs, email (SES/SendGrid), SMS (Twilio) → dễ thêm kênh/đổi nhà cung cấp.',
      'Độ tin cậy: at-least-once + idempotency key để dedup; retry exponential backoff; quá hạn đẩy vào DLQ + alert.',
      'Tôn trọng preferences người dùng (đã opt-out kênh nào) và rate limit của từng provider (token bucket per provider).',
      'Lưu trạng thái từng notification (queued/sent/failed) để truy vết, báo cáo tỉ lệ gửi và thử lại có kiểm soát.',
    ],
  },
  {
    id: 'id-generator',
    title: 'Unique ID Generator phân tán (Snowflake)',
    company: 'Phổ biến · khởi động',
    difficulty: 'Dễ',
    scenario: 'Thiết kế dịch vụ sinh ID duy nhất cho hệ phân tán: nhiều node cùng sinh, không trùng, xấp xỉ tăng dần theo thời gian (sortable), ~10K ID/giây mỗi node, ID vừa trong 64 bit.',
    focus: [
      'Vì sao KHÔNG dùng auto-increment DB hay UUID v4 — thiếu gì?',
      'Cấu trúc bit kiểu Snowflake: timestamp + machine id + sequence',
      'Clock đi lùi (NTP) thì xử lý sao?',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ yêu cầu: duy nhất, sortable theo thời gian, 64-bit, throughput, không cần liên tục', hint: 'Hỏi: cần đoán-không-được không? cần đúng thứ tự tuyệt đối hay xấp xỉ? bao nhiêu ID/s?', w: 14 },
      { k: 'alt', label: 'So sánh phương án: auto-increment, UUID, ticket server, Snowflake', hint: 'Auto-increment: single point + lộ số lượng. UUID v4: 128 bit, không sortable, index B-tree phân mảnh. Ticket server (Flickr): DB cấp lô, vẫn tập trung. Snowflake: 64 bit, sortable, phi tập trung.', w: 22 },
      { k: 'bits', label: 'Thiết kế cấu trúc bit: 41 timestamp (ms) + 10 machine + 12 sequence', hint: '41 bit ms ≈ 69 năm từ epoch tùy chọn; 10 bit = 1024 node; 12 bit = 4096 ID/ms/node. Có thể xê dịch tỉ lệ theo nhu cầu.', w: 22 },
      { k: 'clock', label: 'Xử lý clock skew / clock đi lùi', hint: 'Clock lùi → từ chối sinh (chờ đuổi kịp) hoặc dùng logical clock; NTP slew thay vì step; lưu last-timestamp để phát hiện.', w: 18 },
      { k: 'deploy', label: 'Cấp machine id & vận hành: ZooKeeper/etcd hoặc cấu hình tĩnh', hint: 'Machine id phải duy nhất — cấp qua coordinator lúc khởi động hoặc từ metadata (ordinal của StatefulSet).', w: 12 },
      { k: 'trade', label: 'Trade-offs (sequence tràn trong 1ms, lộ thông tin thời gian, epoch tùy chọn)', hint: 'Tràn 4096/ms → spin chờ ms kế; ID lộ thời điểm tạo — có sao không? Epoch riêng kéo dài tuổi thọ 41 bit.', w: 12 },
    ],
    keyPoints: [
      'Snowflake 64-bit: 1 bit dấu + 41 bit timestamp(ms từ epoch tùy chọn) + 10 bit machine id + 12 bit sequence — sortable theo thời gian, sinh phi tập trung không cần gọi mạng.',
      'UUID v4 tuy đơn giản nhưng 128 bit, không sortable → index B-tree chèn ngẫu nhiên gây phân mảnh; auto-increment DB là single point và lộ business metric.',
      'Clock đi lùi là bug kinh điển: so timestamp hiện tại với last-timestamp, nếu lùi thì từ chối/chờ, không bao giờ sinh trùng.',
      'Sequence 12 bit tràn trong cùng 1ms → busy-wait sang ms kế tiếp; throughput lý thuyết 4096×1000 ID/s/node.',
      'Machine id cấp lúc boot qua ZooKeeper/etcd (ephemeral) hoặc suy từ hạ tầng (StatefulSet ordinal, IP cuối) — phải đảm bảo không trùng.',
    ],
  },
  {
    id: 'web-crawler',
    title: 'Web Crawler (Googlebot-lite)',
    company: 'Google / Search',
    difficulty: 'TB',
    scenario: 'Thiết kế web crawler thu thập 1 tỉ trang/tháng cho search engine: bắt đầu từ seed URLs, tải trang, trích link, tiếp tục crawl. Phải lịch sự với website (politeness), tránh crawl trùng, chịu được trang bẫy.',
    focus: [
      'URL frontier: hàng đợi ưu tiên + politeness (không dập một host)',
      'Dedup: URL đã thấy (Bloom filter) và nội dung trùng (hash/simhash)',
      'Bẫy crawler (spider trap), robots.txt, trang thay đổi thường xuyên',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: quy mô, chỉ HTML hay cả media, độ tươi (freshness), tôn trọng robots.txt', hint: '1 tỉ/tháng ≈ 400 trang/s; hỏi: crawl lại theo chu kỳ? ưu tiên trang nào? lưu nội dung ở đâu cho indexer?', w: 12 },
      { k: 'est', label: 'Ước lượng: QPS, băng thông, dung lượng lưu (trang ~500KB → ~500TB/tháng)', hint: '400 trang/s × 500KB ≈ 200MB/s băng thông; metadata + nội dung nén vào blob storage.', w: 12 },
      { k: 'frontier', label: 'URL Frontier: queue ưu tiên (front) + queue theo host (back) đảm bảo politeness', hint: 'Thiết kế Mercator: front queues theo priority, back queues mỗi host một queue + heap thời-điểm-được-crawl-kế để giãn cách request cùng host.', w: 24 },
      { k: 'dedup', label: 'Chống trùng: Bloom filter cho URL đã thấy, simhash cho nội dung gần trùng', hint: 'Bloom filter chấp nhận false positive (bỏ sót ít URL) đổi lấy RAM nhỏ; nội dung trùng giữa các URL khác nhau rất phổ biến (www vs non-www).', w: 18 },
      { k: 'robust', label: 'Chống bẫy: giới hạn độ sâu/số trang mỗi domain, timeout, blacklist; robots.txt cache', hint: 'Spider trap sinh URL vô hạn (calendar) → cap độ sâu + độ dài URL; tải robots.txt một lần mỗi host và cache có TTL.', w: 18 },
      { k: 'scale', label: 'Phân tán: shard frontier theo hash(host), worker stateless, DNS cache riêng', hint: 'Cùng host phải về cùng shard để politeness đúng; DNS resolve là bottleneck bất ngờ → resolver cache cục bộ.', w: 16 },
    ],
    keyPoints: [
      'Trái tim là URL Frontier kiểu Mercator: ưu tiên (front queues) tách khỏi politeness (back queues per-host + heap thời điểm crawl kế) — một host chỉ bị 1 request tại một thời điểm, có delay giữa 2 lần.',
      'Dedup 2 tầng: URL-seen bằng Bloom filter (RAM nhỏ, chấp nhận false positive), content-seen bằng hash/simhash bắt trang trùng nội dung khác URL.',
      'Shard theo hash(host) chứ không hash(url) — mọi URL cùng host về cùng worker thì politeness mới enforce được.',
      'Robots.txt: tải 1 lần/host, cache có TTL, tôn trọng Crawl-delay; bẫy spider trap chặn bằng cap độ sâu, độ dài URL, số trang/domain.',
      'DNS là bottleneck hay bị quên — 400 lookup/s cần resolver cache riêng, prefetch theo lô.',
      'Freshness: hàng đợi crawl-lại ưu tiên theo tần suất thay đổi ước lượng (news crawl mỗi giờ, trang tĩnh mỗi tháng).',
    ],
  },
  {
    id: 'file-storage',
    title: 'File Storage đồng bộ (Dropbox / Google Drive)',
    company: 'Dropbox',
    difficulty: 'Khó',
    scenario: 'Thiết kế dịch vụ lưu & đồng bộ file đa thiết bị: upload/download file tới 10GB, sửa file trên máy A thì máy B thấy trong vài giây, có lịch sử phiên bản, 500M người dùng.',
    focus: [
      'Chia file thành block/chunk — deduplication + delta sync (chỉ truyền phần đổi)',
      'Metadata service (file→blocks) tách khỏi block storage (S3)',
      'Notify thiết bị khác: long polling / WebSocket; xử lý conflict sửa đồng thời',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: cỡ file max, tần suất sửa, offline rồi sync lại, share/permission có trong scope?', hint: 'Hỏi: file lớn kiểu gì (video?), tỉ lệ đọc/ghi, cần realtime cỡ nào, version giữ bao lâu?', w: 12 },
      { k: 'chunk', label: 'Chunking: chia block 4MB, hash từng block, upload block thiếu', hint: 'Client hash từng block (SHA-256) → hỏi server block nào đã có → chỉ upload block mới. Sửa 1 dòng trong file 1GB chỉ truyền vài block (delta sync).', w: 22 },
      { k: 'meta', label: 'Metadata service: file → danh sách block hash + version; DB quan hệ + cache', hint: 'Bảng file(id, name, owner), version(file_id, seq, block_list), block(hash, size, s3_key). Metadata nhỏ nhưng QPS cao → cache mạnh.', w: 18 },
      { k: 'sync', label: 'Luồng sync: client theo dõi thay đổi cục bộ, notification service báo thiết bị khác kéo về', hint: 'Upload xong → ghi version mới → đẩy event; thiết bị khác nhận qua long-poll/WebSocket rồi tải metadata + block thiếu.', w: 18 },
      { k: 'conflict', label: 'Conflict 2 thiết bị sửa cùng file: version vector / last-write-wins + bản sao conflicted', hint: 'Dropbox tạo file "conflicted copy" thay vì merge; Google Docs mới cần OT/CRDT — nói rõ scope.', w: 16 },
      { k: 'trade', label: 'Trade-offs: dedup toàn cục vs riêng tư, block nhỏ (delta mịn) vs overhead metadata, strong vs eventual', hint: 'Dedup cross-user tiết kiệm khủng nhưng rò rỉ thông tin (biết file đã tồn tại); block nhỏ → bảng block phình.', w: 14 },
    ],
    keyPoints: [
      'Chia file thành block ~4MB + hash: được cùng lúc dedup (block trùng lưu 1 lần), delta sync (chỉ truyền block đổi), resume upload (block nào xong rồi bỏ qua).',
      'Tách 3 mặt phẳng: block storage (S3, bất biến, addressed theo hash) — metadata service (file→blocks, version) — notification service (báo thiết bị kéo sync).',
      'Version = danh sách block hash theo thứ tự → lịch sử phiên bản gần như miễn phí (chỉ tốn block khác nhau).',
      'Conflict đồng thời: đơn giản và an toàn là tạo "conflicted copy" cho bản thua (kiểu Dropbox), tránh merge tự động sai; nêu OT/CRDT là bước nâng cao ngoài scope.',
      'Notify realtime bằng long polling (Dropbox thực tế) hoặc WebSocket; thiết bị offline thì sync lại bằng so sánh version khi online.',
      'Client là một nửa hệ thống: watcher FS, chunker, hasher, local DB trạng thái — phỏng vấn hay quên phần client.',
    ],
  },
  {
    id: 'metrics-monitoring',
    title: 'Hệ thống Metrics & Monitoring (Prometheus-lite / Datadog-lite)',
    company: 'Datadog / SRE',
    difficulty: 'Khó',
    scenario: 'Thiết kế hệ thống thu thập & truy vấn metrics cho 100K server: mỗi máy đẩy CPU/RAM/QPS… 10 metric mỗi 10 giây, dashboard vẽ đồ thị theo thời gian, cảnh báo khi vượt ngưỡng, giữ dữ liệu 1 năm.',
    focus: [
      'Time-series DB: ghi cực nhiều, đọc theo dải thời gian + tag',
      'Pull (Prometheus) vs Push (StatsD/agent) — chọn gì, vì sao',
      'Downsampling & retention: 10s giữ 7 ngày, 1phút giữ 30 ngày, 1giờ giữ 1 năm',
    ],
    rubric: [
      { k: 'req', label: 'Làm rõ: số series, tần suất, độ trễ cảnh báo chấp nhận, truy vấn kiểu gì', hint: '100K máy × 10 metric / 10s = 100K điểm/s; hỏi: cardinality tag (per-endpoint?), alert trễ vài chục giây được không?', w: 12 },
      { k: 'ingest', label: 'Đường ghi: agent → gateway → Kafka đệm → consumer ghi TSDB', hint: 'Kafka hấp thụ đỉnh tải + cho phép nhiều consumer (TSDB, alert engine) đọc cùng luồng; agent batch + nén.', w: 18 },
      { k: 'storage', label: 'TSDB: append-only, nén delta-of-delta + XOR (Gorilla), shard theo metric+tag, index tag→series', hint: 'Điểm (timestamp,value) nén còn ~1.4 byte (Facebook Gorilla); dữ liệu nóng trong RAM/SSD, nguội đẩy object storage.', w: 22 },
      { k: 'query', label: 'Đường đọc: query theo dải thời gian + lọc tag + hàm gộp (avg/p95/rate)', hint: 'Dashboard chỉ cần ~vài trăm điểm/đồ thị → gộp phía server, đừng trả raw; cache kết quả panel hay xem.', w: 14 },
      { k: 'retention', label: 'Downsampling + retention nhiều tầng', hint: 'Job nền cuộn 10s→1m→1h; xóa raw sau 7 ngày. Trade-off: mất chi tiết cũ đổi lấy 1 năm dữ liệu rẻ.', w: 16 },
      { k: 'alert', label: 'Alert engine: rule chạy định kỳ trên luồng/TSDB, chống flapping, notification tách riêng', hint: 'Đánh giá rule mỗi 10-30s; "for 5m" tránh cảnh báo nhấp nháy; gửi qua hệ notification (đề trước!) có dedup.', w: 18 },
    ],
    keyPoints: [
      'Workload đặc thù: ghi khủng khiếp (100K điểm/s) append-only, đọc theo dải thời gian gần đây → TSDB chuyên dụng, không nhét vào RDBMS.',
      'Nén kiểu Gorilla (delta-of-delta timestamp + XOR value) đưa mỗi điểm về ~1.4 byte — là lý do TSDB sống được với quy mô này.',
      'Kafka làm đệm giữa ingest và storage: hấp thụ burst, replay được, và alert engine đọc CÙNG luồng mà không đụng đường ghi.',
      'Cardinality là kẻ thù số 1: tag không kiểm soát (user_id trong tag!) làm nổ số series — giới hạn và validate tag từ gateway.',
      'Retention nhiều tầng: raw 10s giữ 7 ngày → cuộn 1 phút giữ 30 ngày → 1 giờ giữ 1 năm; job downsample chạy nền.',
      'Pull (Prometheus, dễ phát hiện máy chết, control ở server) vs Push (qua NAT/serverless tốt) — chọn theo môi trường, nêu được cả hai là điểm cộng.',
    ],
  },
];
