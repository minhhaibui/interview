/**
 * Ngân hàng "⏱️ Độ phức tạp" — nhìn đoạn code, chọn Big-O đúng.
 * Sát vòng phỏng vấn: sau khi giải xong bài, interviewer luôn hỏi
 * "độ phức tạp thời gian/bộ nhớ của lời giải là bao nhiêu?".
 *
 * Mỗi câu: { id, topic, kind:'time'|'space', code, q, options:[Big-O], answer:idx, explain }
 * Các option đều là ký hiệu Big-O ngắn bằng nhau ⇒ KHÔNG lộ đáp án qua độ dài.
 */
window.COMPLEXITY_QUIZ = [
  {
    id: 'cx-single-loop', topic: 'Vòng lặp', kind: 'time',
    code: `function sum(arr) {\n  let s = 0;\n  for (let i = 0; i < arr.length; i++) s += arr[i];\n  return s;\n}`,
    q: 'Độ phức tạp THỜI GIAN của hàm trên (n = arr.length)?',
    options: ['O(1)', 'O(log n)', 'O(n)', 'O(n²)'], answer: 2,
    explain: 'Một vòng lặp chạy đúng n lần, mỗi lần làm việc O(1) ⇒ O(n).',
  },
  {
    id: 'cx-two-seq-loops', topic: 'Vòng lặp', kind: 'time',
    code: `function f(arr) {\n  let a = 0, b = 1;\n  for (const x of arr) a += x;\n  for (const x of arr) b *= x;\n  return [a, b];\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n)', 'O(2n log n)', 'O(n²)', 'O(log n)'], answer: 0,
    explain: 'Hai vòng lặp NỐI TIẾP nhau ⇒ n + n = 2n, hằng số bị bỏ ⇒ O(n). Chỉ vòng lặp LỒNG nhau mới nhân lên.',
  },
  {
    id: 'cx-nested-loop', topic: 'Vòng lặp lồng', kind: 'time',
    code: `function pairs(arr) {\n  const out = [];\n  for (let i = 0; i < arr.length; i++)\n    for (let j = 0; j < arr.length; j++)\n      out.push([arr[i], arr[j]]);\n  return out;\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n)', 'O(n log n)', 'O(n²)', 'O(n³)'], answer: 2,
    explain: 'Vòng ngoài n lần, mỗi lần vòng trong chạy n lần ⇒ n × n = O(n²).',
  },
  {
    id: 'cx-triangular', topic: 'Vòng lặp lồng', kind: 'time',
    code: `function f(arr) {\n  let c = 0;\n  for (let i = 0; i < arr.length; i++)\n    for (let j = i + 1; j < arr.length; j++)\n      c++;\n  return c;\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n)', 'O(n log n)', 'O(n²)', 'O(n!)'], answer: 2,
    explain: 'Số lần lặp = n(n−1)/2 ≈ n²/2. Bỏ hằng số 1/2 ⇒ O(n²) — dù vòng trong "ngắn dần" thì bậc vẫn là n².',
  },
  {
    id: 'cx-inner-const', topic: 'Vòng lặp lồng', kind: 'time',
    code: `function f(arr) {\n  let c = 0;\n  for (let i = 0; i < arr.length; i++)\n    for (let j = 0; j < 100; j++)\n      c += arr[i] * j;\n  return c;\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n)', 'O(100n²)', 'O(n²)', 'O(1)'], answer: 0,
    explain: 'Vòng trong chạy 100 lần — HẰNG SỐ, không phụ thuộc n ⇒ 100n = O(n). Lồng nhau chưa chắc là O(n²).',
  },
  {
    id: 'cx-halving', topic: 'Chia đôi', kind: 'time',
    code: `function f(n) {\n  let c = 0;\n  while (n > 1) { n = Math.floor(n / 2); c++; }\n  return c;\n}`,
    q: 'Độ phức tạp THỜI GIAN theo n?',
    options: ['O(n)', 'O(log n)', 'O(√n)', 'O(n log n)'], answer: 1,
    explain: 'Mỗi vòng n giảm một nửa ⇒ số vòng ≈ log₂n ⇒ O(log n).',
  },
  {
    id: 'cx-doubling', topic: 'Chia đôi', kind: 'time',
    code: `function f(n) {\n  let c = 0;\n  for (let i = 1; i < n; i *= 2) c++;\n  return c;\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n)', 'O(n/2)', 'O(log n)', 'O(√n)'], answer: 2,
    explain: 'i nhân đôi mỗi vòng (1, 2, 4, 8…) nên tới n chỉ mất log₂n bước ⇒ O(log n).',
  },
  {
    id: 'cx-binary-search', topic: 'Tìm kiếm', kind: 'time',
    code: `function bsearch(a, x) {\n  let lo = 0, hi = a.length - 1;\n  while (lo <= hi) {\n    const mid = (lo + hi) >> 1;\n    if (a[mid] === x) return mid;\n    a[mid] < x ? lo = mid + 1 : hi = mid - 1;\n  }\n  return -1;\n}`,
    q: 'Độ phức tạp THỜI GIAN (mảng đã sắp xếp, n phần tử)?',
    options: ['O(1)', 'O(log n)', 'O(n)', 'O(n log n)'], answer: 1,
    explain: 'Mỗi bước loại bỏ nửa khoảng tìm kiếm ⇒ O(log n). Điều kiện bắt buộc: mảng đã sắp xếp sẵn.',
  },
  {
    id: 'cx-outer-log', topic: 'Vòng lặp lồng', kind: 'time',
    code: `function f(arr) {\n  let c = 0;\n  for (let i = 1; i < arr.length; i *= 2)\n    for (let j = 0; j < arr.length; j++)\n      c++;\n  return c;\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n log n)', 'O(n²)', 'O(log n)', 'O(n)'], answer: 0,
    explain: 'Vòng ngoài log n lần (i nhân đôi), vòng trong n lần ⇒ n × log n = O(n log n).',
  },
  {
    id: 'cx-sort-then-scan', topic: 'Sắp xếp', kind: 'time',
    code: `function f(arr) {\n  const s = [...arr].sort((a, b) => a - b);\n  let best = 0;\n  for (let i = 1; i < s.length; i++)\n    best = Math.max(best, s[i] - s[i - 1]);\n  return best;\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n)', 'O(n log n)', 'O(n²)', 'O(log n)'], answer: 1,
    explain: 'sort() là O(n log n), quét sau đó là O(n). Tổng = O(n log n + n) ⇒ lấy số hạng lớn nhất: O(n log n).',
  },
  {
    id: 'cx-includes-in-loop', topic: 'Cấu trúc dữ liệu', kind: 'time',
    code: `function common(a, b) {\n  const out = [];\n  for (const x of a)\n    if (b.includes(x)) out.push(x);\n  return out;\n}`,
    q: 'Độ phức tạp THỜI GIAN (|a| = |b| = n)?',
    options: ['O(n)', 'O(n log n)', 'O(n²)', 'O(2n)'], answer: 2,
    explain: 'Array.includes() phải quét tuyến tính ⇒ O(n) BÊN TRONG vòng lặp n lần ⇒ O(n²). Lỗi kinh điển khi phỏng vấn.',
  },
  {
    id: 'cx-set-lookup', topic: 'Cấu trúc dữ liệu', kind: 'time',
    code: `function common(a, b) {\n  const set = new Set(b);\n  const out = [];\n  for (const x of a)\n    if (set.has(x)) out.push(x);\n  return out;\n}`,
    q: 'Độ phức tạp THỜI GIAN (|a| = |b| = n)?',
    options: ['O(n)', 'O(n log n)', 'O(n²)', 'O(log n)'], answer: 0,
    explain: 'Dựng Set mất O(n), mỗi set.has() trung bình O(1) ⇒ tổng O(n). Đây chính là cách tối ưu bài trên từ O(n²) xuống O(n).',
  },
  {
    id: 'cx-shift-loop', topic: 'Thao tác mảng', kind: 'time',
    code: `function drain(arr) {\n  const out = [];\n  while (arr.length) out.push(arr.shift());\n  return out;\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n)', 'O(n²)', 'O(n log n)', 'O(1)'], answer: 1,
    explain: 'shift() phải dịch TOÀN BỘ phần tử còn lại sang trái ⇒ O(n) mỗi lần, lặp n lần ⇒ O(n²). Dùng pop() (O(1)) hoặc con trỏ index thì còn O(n).',
  },
  {
    id: 'cx-push-amortized', topic: 'Thao tác mảng', kind: 'time',
    code: `function build(n) {\n  const out = [];\n  for (let i = 0; i < n; i++) out.push(i * i);\n  return out;\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n)', 'O(n²)', 'O(log n)', 'O(n log n)'], answer: 0,
    explain: 'push() là O(1) khấu hao (amortized) — thỉnh thoảng phải cấp phát lại mảng nhưng chia đều ra vẫn là O(1) ⇒ tổng O(n).',
  },
  {
    id: 'cx-unshift-loop', topic: 'Thao tác mảng', kind: 'time',
    code: `function reverse(arr) {\n  const out = [];\n  for (const x of arr) out.unshift(x);\n  return out;\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n)', 'O(n log n)', 'O(n²)', 'O(1)'], answer: 2,
    explain: 'unshift() chèn đầu mảng ⇒ dịch mọi phần tử, O(n) mỗi lần × n lần = O(n²). Dùng push() rồi reverse() sẽ là O(n).',
  },
  {
    id: 'cx-splice-in-loop', topic: 'Thao tác mảng', kind: 'time',
    code: `function removeAll(arr, x) {\n  for (let i = arr.length - 1; i >= 0; i--)\n    if (arr[i] === x) arr.splice(i, 1);\n  return arr;\n}`,
    q: 'Độ phức tạp THỜI GIAN trong TRƯỜNG HỢP XẤU NHẤT?',
    options: ['O(n)', 'O(n²)', 'O(n log n)', 'O(log n)'], answer: 1,
    explain: 'Mỗi splice() dịch mảng ⇒ O(n); xấu nhất mọi phần tử đều bằng x ⇒ n lần splice ⇒ O(n²). Dùng filter() sẽ là O(n).',
  },
  {
    id: 'cx-matrix', topic: 'Ma trận', kind: 'time',
    code: `function total(grid) { // grid: n × m\n  let s = 0;\n  for (let i = 0; i < grid.length; i++)\n    for (let j = 0; j < grid[i].length; j++)\n      s += grid[i][j];\n  return s;\n}`,
    q: 'Độ phức tạp THỜI GIAN với ma trận n hàng × m cột?',
    options: ['O(n)', 'O(n × m)', 'O(n²)', 'O(n + m)'], answer: 1,
    explain: 'Duyệt đúng mỗi ô một lần ⇒ O(n·m). Chỉ khi n = m mới viết gọn thành O(n²) — nhớ phân biệt 2 biến.',
  },
  {
    id: 'cx-triple-nested', topic: 'Vòng lặp lồng', kind: 'time',
    code: `function triples(a) {\n  let c = 0;\n  for (let i = 0; i < a.length; i++)\n    for (let j = 0; j < a.length; j++)\n      for (let k = 0; k < a.length; k++)\n        if (a[i] + a[j] + a[k] === 0) c++;\n  return c;\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n²)', 'O(n³)', 'O(n log n)', 'O(3n)'], answer: 1,
    explain: '3 vòng lặp lồng nhau, mỗi vòng n lần ⇒ n³. (Bài 3Sum tối ưu được xuống O(n²) bằng sort + hai con trỏ.)',
  },
  {
    id: 'cx-fib-naive', topic: 'Đệ quy', kind: 'time',
    code: `function fib(n) {\n  if (n <= 1) return n;\n  return fib(n - 1) + fib(n - 2);\n}`,
    q: 'Độ phức tạp THỜI GIAN theo n?',
    options: ['O(n)', 'O(n²)', 'O(2ⁿ)', 'O(n log n)'], answer: 2,
    explain: 'Mỗi lời gọi sinh 2 lời gọi con ⇒ cây đệ quy nhân đôi theo độ sâu n ⇒ O(2ⁿ) (chính xác hơn là O(φⁿ)).',
  },
  {
    id: 'cx-fib-memo', topic: 'Đệ quy', kind: 'time',
    code: `function fib(n, memo = {}) {\n  if (n <= 1) return n;\n  if (memo[n] !== undefined) return memo[n];\n  return memo[n] = fib(n - 1, memo) + fib(n - 2, memo);\n}`,
    q: 'Độ phức tạp THỜI GIAN sau khi memo hoá?',
    options: ['O(n)', 'O(2ⁿ)', 'O(n²)', 'O(log n)'], answer: 0,
    explain: 'Mỗi giá trị n chỉ tính ĐÚNG MỘT LẦN rồi lưu lại ⇒ n phép tính O(1) ⇒ O(n). Đây là ý chính của dynamic programming.',
  },
  {
    id: 'cx-merge-sort', topic: 'Chia để trị', kind: 'time',
    code: `function mergeSort(a) {\n  if (a.length <= 1) return a;\n  const mid = a.length >> 1;\n  const l = mergeSort(a.slice(0, mid));\n  const r = mergeSort(a.slice(mid));\n  return merge(l, r); // merge: O(n)\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n)', 'O(n log n)', 'O(n²)', 'O(log n)'], answer: 1,
    explain: 'T(n) = 2T(n/2) + O(n): log n tầng đệ quy, mỗi tầng trộn tốn O(n) ⇒ O(n log n).',
  },
  {
    id: 'cx-quicksort-worst', topic: 'Sắp xếp', kind: 'time',
    code: `// quicksort luôn chọn phần tử ĐẦU làm pivot,\n// đầu vào là mảng ĐÃ sắp xếp tăng dần\nquickSort([1, 2, 3, 4, 5, 6, 7, 8]);`,
    q: 'Độ phức tạp THỜI GIAN trong tình huống này?',
    options: ['O(n log n)', 'O(n)', 'O(n²)', 'O(log n)'], answer: 2,
    explain: 'Pivot tệ ⇒ mỗi lần chia chỉ tách ra 1 phần tử, đệ quy sâu n tầng, mỗi tầng O(n) ⇒ O(n²). Trung bình mới là O(n log n) — nhớ phân biệt average vs worst case.',
  },
  {
    id: 'cx-heap-topk', topic: 'Cấu trúc dữ liệu', kind: 'time',
    code: `function topK(a, k) {\n  const heap = new MinHeap(); // push/pop: O(log k)\n  for (const x of a) {\n    heap.push(x);\n    if (heap.size() > k) heap.pop();\n  }\n  return heap.toArray();\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n log k)', 'O(n log n)', 'O(k log n)', 'O(n × k)'], answer: 0,
    explain: 'n phần tử, mỗi phần tử làm việc trên heap kích thước tối đa k ⇒ O(n log k) — tốt hơn sort toàn mảng O(n log n) khi k nhỏ.',
  },
  {
    id: 'cx-bfs-graph', topic: 'Đồ thị', kind: 'time',
    code: `function bfs(adj, start) { // adj: danh sách kề\n  const seen = new Set([start]);\n  const q = [start];\n  for (let i = 0; i < q.length; i++)\n    for (const nb of adj[q[i]] || [])\n      if (!seen.has(nb)) { seen.add(nb); q.push(nb); }\n  return seen;\n}`,
    q: 'Độ phức tạp THỜI GIAN (V đỉnh, E cạnh)?',
    options: ['O(V × E)', 'O(V + E)', 'O(V²)', 'O(E log V)'], answer: 1,
    explain: 'Mỗi đỉnh vào hàng đợi tối đa 1 lần, mỗi cạnh được duyệt tối đa 1 lần (2 lần nếu vô hướng) ⇒ O(V + E) với danh sách kề.',
  },
  {
    id: 'cx-tree-traverse', topic: 'Đệ quy', kind: 'time',
    code: `function inorder(node, out = []) {\n  if (!node) return out;\n  inorder(node.left, out);\n  out.push(node.val);\n  inorder(node.right, out);\n  return out;\n}`,
    q: 'Độ phức tạp THỜI GIAN với cây n nút?',
    options: ['O(log n)', 'O(n)', 'O(n log n)', 'O(h)'], answer: 1,
    explain: 'Mỗi nút được thăm đúng 1 lần với O(1) việc ⇒ O(n), bất kể cây cân bằng hay lệch.',
  },
  {
    id: 'cx-subsets', topic: 'Sinh tổ hợp', kind: 'time',
    code: `function subsets(a) {\n  let res = [[]];\n  for (const x of a)\n    res = res.concat(res.map(s => [...s, x]));\n  return res;\n}`,
    q: 'Độ phức tạp THỜI GIAN (đếm theo số tập con sinh ra)?',
    options: ['O(n²)', 'O(n!)', 'O(2ⁿ)', 'O(n log n)'], answer: 2,
    explain: 'Mảng n phần tử có 2ⁿ tập con — chỉ riêng việc liệt kê đã là O(2ⁿ), không thể nhanh hơn output.',
  },
  {
    id: 'cx-permutations', topic: 'Sinh tổ hợp', kind: 'time',
    code: `function permute(a) {\n  if (a.length <= 1) return [a];\n  return a.flatMap((x, i) =>\n    permute([...a.slice(0, i), ...a.slice(i + 1)])\n      .map(p => [x, ...p]));\n}`,
    q: 'Độ phức tạp THỜI GIAN (đếm theo số hoán vị sinh ra)?',
    options: ['O(2ⁿ)', 'O(n!)', 'O(n³)', 'O(nⁿ)'], answer: 1,
    explain: 'Có n! hoán vị nên chỉ liệt kê thôi đã là O(n!) (thực tế O(n × n!) vì mỗi hoán vị dài n).',
  },
  {
    id: 'cx-sliding-window', topic: 'Hai con trỏ', kind: 'time',
    code: `function longestUnique(s) {\n  const seen = new Map();\n  let lo = 0, best = 0;\n  for (let hi = 0; hi < s.length; hi++) {\n    const c = s[hi];\n    if (seen.has(c) && seen.get(c) >= lo) lo = seen.get(c) + 1;\n    seen.set(c, hi);\n    best = Math.max(best, hi - lo + 1);\n  }\n  return best;\n}`,
    q: 'Độ phức tạp THỜI GIAN?',
    options: ['O(n)', 'O(n²)', 'O(n log n)', 'O(k × n)'], answer: 0,
    explain: 'Sliding window: mỗi con trỏ lo/hi chỉ tiến về phía trước, tổng cộng ≤ 2n bước, Map tra O(1) ⇒ O(n).',
  },
  {
    id: 'cx-string-build', topic: 'Chuỗi', kind: 'time',
    code: `function repeatJoin(words) {\n  let out = '';\n  for (const w of words) out = out + w + ',';\n  return out;\n}`,
    q: 'Theo mô hình lý thuyết (chuỗi bất biến, mỗi lần nối phải copy), độ phức tạp là?',
    options: ['O(n)', 'O(n²)', 'O(n log n)', 'O(1)'], answer: 1,
    explain: 'Chuỗi immutable: mỗi lần nối phải copy toàn bộ chuỗi đã có ⇒ 1+2+…+n ≈ n²/2 ⇒ O(n²). Vì vậy Java khuyên StringBuilder, JS khuyên push vào mảng rồi join() ⇒ O(n).',
  },
  {
    id: 'cx-nested-different', topic: 'Vòng lặp lồng', kind: 'time',
    code: `function f(a, b) {\n  let c = 0;\n  for (const x of a)\n    for (const y of b)\n      if (x === y) c++;\n  return c;\n}`,
    q: 'Độ phức tạp THỜI GIAN với |a| = n, |b| = m?',
    options: ['O(n + m)', 'O(n × m)', 'O(n²)', 'O(max(n, m))'], answer: 1,
    explain: 'Hai mảng KHÁC nhau ⇒ O(n·m), không được viết ẩu thành O(n²). Ghi rõ từng biến là điểm cộng khi phỏng vấn.',
  },
  {
    id: 'cx-early-return', topic: 'Trường hợp xấu nhất', kind: 'time',
    code: `function hasDup(a) {\n  for (let i = 0; i < a.length; i++)\n    for (let j = i + 1; j < a.length; j++)\n      if (a[i] === a[j]) return true;\n  return false;\n}`,
    q: 'Độ phức tạp THỜI GIAN trong TRƯỜNG HỢP XẤU NHẤT?',
    options: ['O(n)', 'O(n²)', 'O(n log n)', 'O(1)'], answer: 1,
    explain: 'Xấu nhất (không có phần tử trùng) phải duyệt hết mọi cặp ⇒ O(n²). Return sớm chỉ cải thiện best case — Big-O mặc định nói về worst case.',
  },
  {
    id: 'cx-space-inplace', topic: 'Bộ nhớ', kind: 'space',
    code: `function reverseInPlace(a) {\n  for (let i = 0, j = a.length - 1; i < j; i++, j--) {\n    const t = a[i]; a[i] = a[j]; a[j] = t;\n  }\n  return a;\n}`,
    q: 'Độ phức tạp BỘ NHỚ phụ (auxiliary space)?',
    options: ['O(1)', 'O(n)', 'O(log n)', 'O(n²)'], answer: 0,
    explain: 'Chỉ dùng vài biến i, j, t bất kể n lớn cỡ nào ⇒ O(1). Mảng đầu vào không tính vào auxiliary space.',
  },
  {
    id: 'cx-space-map', topic: 'Bộ nhớ', kind: 'space',
    code: `function freq(a) {\n  const m = new Map();\n  for (const x of a) m.set(x, (m.get(x) || 0) + 1);\n  return m;\n}`,
    q: 'Độ phức tạp BỘ NHỚ trong trường hợp xấu nhất?',
    options: ['O(1)', 'O(n)', 'O(log n)', 'O(n²)'], answer: 1,
    explain: 'Xấu nhất mọi phần tử đều khác nhau ⇒ Map giữ n cặp key/value ⇒ O(n). (Thời gian vẫn là O(n).)',
  },
  {
    id: 'cx-space-recursion', topic: 'Bộ nhớ', kind: 'space',
    code: `function sumTo(n) {\n  if (n === 0) return 0;\n  return n + sumTo(n - 1);\n}`,
    q: 'Độ phức tạp BỘ NHỚ?',
    options: ['O(1)', 'O(n)', 'O(log n)', 'O(2ⁿ)'], answer: 1,
    explain: 'Đệ quy sâu n tầng ⇒ n stack frame chờ cùng lúc ⇒ O(n) bộ nhớ (và có thể stack overflow). Viết bằng vòng lặp sẽ là O(1).',
  },
  {
    id: 'cx-space-mergesort', topic: 'Bộ nhớ', kind: 'space',
    code: `// merge sort kinh điển: mỗi lần merge tạo mảng mới\nconst l = mergeSort(a.slice(0, mid));\nconst r = mergeSort(a.slice(mid));\nreturn merge(l, r);`,
    q: 'Độ phức tạp BỘ NHỚ phụ của merge sort trên mảng?',
    options: ['O(1)', 'O(log n)', 'O(n)', 'O(n log n)'], answer: 2,
    explain: 'Cần mảng tạm cỡ n để trộn ⇒ O(n) (cộng O(log n) stack). Đây là điểm yếu của merge sort so với quicksort in-place O(log n).',
  },
  {
    id: 'cx-space-binary-tree', topic: 'Bộ nhớ', kind: 'space',
    code: `function depth(node) {\n  if (!node) return 0;\n  return 1 + Math.max(depth(node.left), depth(node.right));\n}`,
    q: 'Độ phức tạp BỘ NHỚ với cây CÂN BẰNG n nút?',
    options: ['O(1)', 'O(log n)', 'O(n)', 'O(n log n)'], answer: 1,
    explain: 'Bộ nhớ = độ sâu đệ quy = chiều cao cây. Cây cân bằng có h ≈ log n ⇒ O(log n); cây lệch thành danh sách thì thành O(n).',
  },
  {
    id: 'cx-hashmap-worst', topic: 'Cấu trúc dữ liệu', kind: 'time',
    code: `// Tra cứu một key trong HashMap/Map\nmap.get(key);`,
    q: 'Độ phức tạp THỜI GIAN của một lần get trong TRƯỜNG HỢP XẤU NHẤT (mọi key đụng độ cùng bucket, bucket là danh sách liên kết)?',
    options: ['O(1)', 'O(log n)', 'O(n)', 'O(n log n)'], answer: 2,
    explain: 'Trung bình O(1), nhưng nếu hash xấu khiến mọi key rơi cùng bucket thì phải quét cả chuỗi ⇒ O(n). (Java 8+ chuyển bucket dài sang cây đỏ-đen ⇒ O(log n).)',
  },
  {
    id: 'cx-sort-in-loop', topic: 'Sắp xếp', kind: 'time',
    code: `function f(rows) {\n  for (const r of rows) {\n    const s = [...r.items].sort();\n    r.first = s[0];\n  }\n  return rows;\n}`,
    q: 'Độ phức tạp THỜI GIAN với n hàng, mỗi hàng m phần tử?',
    options: ['O(n + m log m)', 'O(n × m log m)', 'O(n log n)', 'O(n × m)'], answer: 1,
    explain: 'Mỗi hàng sort tốn O(m log m), lặp n hàng ⇒ O(n·m log m). (Ở đây chỉ cần Math.min ⇒ hạ xuống O(n·m).)',
  },
];
